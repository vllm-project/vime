diff --git a/megatron/core/datasets/data_schedule.py b/megatron/core/datasets/data_schedule.py
old mode 100644
new mode 100755
index 0f01647..741efbb
--- a/megatron/core/datasets/data_schedule.py
+++ b/megatron/core/datasets/data_schedule.py
@@ -208,12 +208,12 @@ class HybridCPDataLoaderWrapper:
         def _pack_sample_by_key(key: str) -> torch.Tensor:
             flattened_tensors = []
             for gid in send_ids_sorted:
-                t = batch[gid2local_id[gid]][key].to(torch.cuda.current_device(), non_blocking=True)
+                t = batch[gid2local_id[gid]][key].to(torch.npu.current_device(), non_blocking=True)
                 flattened_tensors.append(t)
             return (
                 torch.cat(flattened_tensors, dim=0)
                 if flattened_tensors
-                else torch.empty(0, device=torch.cuda.current_device(), dtype=batch[0][key].dtype)
+                else torch.empty(0, device=torch.npu.current_device(), dtype=batch[0][key].dtype)
             )
 
         def _unpack_sample_by_key(key: str, recv_tensor: torch.Tensor):
@@ -226,7 +226,7 @@ class HybridCPDataLoaderWrapper:
         for key in data_keys:
             send_tensor = _pack_sample_by_key(key)
             recv_tensor = torch.empty(
-                sum(recv_lens_split), device=torch.cuda.current_device(), dtype=send_tensor.dtype
+                sum(recv_lens_split), device=torch.npu.current_device(), dtype=send_tensor.dtype
             )
             torch.distributed.all_to_all_single(
                 output=recv_tensor,
diff --git a/megatron/core/dist_checkpointing/strategies/async_utils.py b/megatron/core/dist_checkpointing/strategies/async_utils.py
old mode 100644
new mode 100755
index 85941fd..ffb36ae
--- a/megatron/core/dist_checkpointing/strategies/async_utils.py
+++ b/megatron/core/dist_checkpointing/strategies/async_utils.py
@@ -162,7 +162,7 @@ class AsyncCaller(ABC):
             bool: True if all ranks are done, False if at least one rank is still active.
 
         """
-        ten = torch.tensor([is_alive], dtype=torch.int, device=torch.cuda.current_device())
+        ten = torch.tensor([is_alive], dtype=torch.int, device=torch.npu.current_device())
         torch.distributed.all_reduce(ten)
         return ten[0] == 0
 
@@ -598,7 +598,7 @@ class AsyncCallsQueue:
                 call_idx, _, async_request = self.async_calls.popleft()
                 for finalize_fn in async_request.finalize_fns:
                     finalize_fn()
-                ten = torch.tensor([call_idx], dtype=torch.int, device=torch.cuda.current_device())
+                ten = torch.tensor([call_idx], dtype=torch.int, device=torch.npu.current_device())
                 torch.distributed.all_reduce(ten, op=torch.distributed.ReduceOp.MAX)
                 assert ten.item() == call_idx, "Unmatched async calls. "
                 "That probably means not all ranks are participating in async finalization"
diff --git a/megatron/core/dist_checkpointing/strategies/state_dict_saver.py b/megatron/core/dist_checkpointing/strategies/state_dict_saver.py
old mode 100644
new mode 100755
index 31ed0bb..835c175
--- a/megatron/core/dist_checkpointing/strategies/state_dict_saver.py
+++ b/megatron/core/dist_checkpointing/strategies/state_dict_saver.py
@@ -249,7 +249,7 @@ def save_state_dict_async_finalize(
     # Broadcast failure status to all ranks to raise exceptions everywhere if needed.
     # The failure details are only raised on the coordinator.
     failures_occurred = torch.tensor(
-        [int(len(node_failures) > 0)], dtype=torch.int, device=torch.cuda.current_device()
+        [int(len(node_failures) > 0)], dtype=torch.int, device=torch.npu.current_device()
     )
     torch.distributed.broadcast(
         failures_occurred, src=dist_wrapper.coordinator_rank, group=dist_wrapper.group
diff --git a/megatron/core/distributed/distributed_data_parallel.py b/megatron/core/distributed/distributed_data_parallel.py
old mode 100644
new mode 100755
index 55179ff..ca7a00b
--- a/megatron/core/distributed/distributed_data_parallel.py
+++ b/megatron/core/distributed/distributed_data_parallel.py
@@ -227,7 +227,7 @@ class DistributedDataParallel(_BaseDataParallel):
                 assert (
                     self.ddp_config.use_distributed_optimizer
                 ), 'Partial DistOpt cannot be used without DistOpt'
-                communication_stream = torch.cuda.Stream(device=torch.cuda.current_device())
+                communication_stream = torch.cuda.Stream(device=torch.npu.current_device())
                 for bucket_group in bucket_groups:
                     bucket_group.inter_distributed_optimizer_instance_group = (
                         self.inter_dist_opt_group
diff --git a/megatron/core/distributed/fsdp/mcore_fsdp_adapter.py b/megatron/core/distributed/fsdp/mcore_fsdp_adapter.py
old mode 100644
new mode 100755
index d6384e70..fe42206
--- a/megatron/core/distributed/fsdp/mcore_fsdp_adapter.py
+++ b/megatron/core/distributed/fsdp/mcore_fsdp_adapter.py
@@ -88,7 +88,7 @@ class FullyShardedDataParallel(_BaseDataParallel):
         self.bucket_size = self.ddp_config.bucket_size
         if disable_bucketing:
             self.bucket_size = None
-        self.device = device if device else torch.device(f'cuda:{torch.cuda.current_device()}')
+        self.device = device if device else torch.device(f'cuda:{torch.npu.current_device()}')
 
         if fsdp_unit_modules is not None:
             self.fsdp_unit_modules = fsdp_unit_modules
@@ -421,7 +421,7 @@ def _get_rng_state_dict():
         'random_rng_state': random.getstate(),
         'np_rng_state': np.random.get_state(),
         'torch_rng_state': torch.get_rng_state(),
-        'cuda_rng_state': torch.cuda.get_rng_state(),
+        'cuda_rng_state': torch.npu.get_rng_state(),
         'rng_tracker_states': tensor_parallel.get_cuda_rng_tracker().get_states(),
     }
     return rng_state_dict
@@ -431,5 +431,5 @@ def _load_rng_state_dict(rng_state_dict):
     random.setstate(rng_state_dict['random_rng_state'])
     np.random.set_state(rng_state_dict['np_rng_state'])
     torch.set_rng_state(rng_state_dict['torch_rng_state'])
-    torch.cuda.set_rng_state(rng_state_dict['cuda_rng_state'])
+    torch.npu.set_rng_state(rng_state_dict['cuda_rng_state'])
     tensor_parallel.get_cuda_rng_tracker().set_states(rng_state_dict['rng_tracker_states'])
diff --git a/megatron/core/distributed/fsdp/src/megatron_fsdp/megatron_fsdp.py b/megatron/core/distributed/fsdp/src/megatron_fsdp/megatron_fsdp.py
old mode 100644
new mode 100755
index bd13e76..4c72dfe
--- a/megatron/core/distributed/fsdp/src/megatron_fsdp/megatron_fsdp.py
+++ b/megatron/core/distributed/fsdp/src/megatron_fsdp/megatron_fsdp.py
@@ -149,7 +149,7 @@ class MegatronFSDP(torch.nn.Module):
         ...     dist_index,
         ...     ddp_config,
         ...     fsdp_unit_modules = [TransformerLayer, LanguageModelEmbedding],
-        ...     device=torch.device(f"cuda:{torch.cuda.current_device()}"),
+        ...     device=torch.device(f"cuda:{torch.npu.current_device()}"),
         ...     init_model_with_meta_device=False,
         ...     disable_bucketing=False,
         ...     keep_fp8_transpose_cache=False,
@@ -179,13 +179,13 @@ class MegatronFSDP(torch.nn.Module):
         super().__init__()
         # If device is not specified, use the current device.
         self.device = (
-            device if device is not None else torch.device(f"cuda:{torch.cuda.current_device()}")
+            device if device is not None else torch.device(f"cuda:{torch.npu.current_device()}")
         )
-        if self.device != torch.device(f"cuda:{torch.cuda.current_device()}"):
+        if self.device != torch.device(f"cuda:{torch.npu.current_device()}"):
             logger.warning(
                 f"[Rank {torch.distributed.get_rank()}] Megatron-FSDP is "
                 f"using device {self.device} instead of the current device "
-                f"{torch.device(f'cuda:{torch.cuda.current_device()}')}, "
+                f"{torch.device(f'cuda:{torch.npu.current_device()}')}, "
                 "which may cause process-to-device mapping issues or "
                 "cross-device Tensor operation errors. If necessary, "
                 "send all Tensors in the module to the Megatron-FSDP "
diff --git a/megatron/core/distributed/fsdp/src/megatron_fsdp/param_and_grad_buffer.py b/megatron/core/distributed/fsdp/src/megatron_fsdp/param_and_grad_buffer.py
old mode 100644
new mode 100755
index 3ec117e..40c465b
--- a/megatron/core/distributed/fsdp/src/megatron_fsdp/param_and_grad_buffer.py
+++ b/megatron/core/distributed/fsdp/src/megatron_fsdp/param_and_grad_buffer.py
@@ -214,7 +214,7 @@ class MultiGroupUBRAllocator:
 
     def __enter__(self):
         for group in self.groups[1:]:
-            backend = group._get_backend(torch.device("cuda", torch.cuda.current_device()))
+            backend = group._get_backend(torch.device("cuda", torch.npu.current_device()))
             try:
                 # Since the registration is done in mempool granularity, we need to deregister
                 # the tensors in the mempool and re-register the mempool including the newly created
@@ -227,7 +227,7 @@ class MultiGroupUBRAllocator:
     def __exit__(self, *args):
         self.mem_allocator.__exit__(*args)
         for group in self.groups[1:]:
-            backend = group._get_backend(torch.device("cuda", torch.cuda.current_device()))
+            backend = group._get_backend(torch.device("cuda", torch.npu.current_device()))
             log_single_rank(
                 logger,
                 logging.INFO,
@@ -3788,7 +3788,7 @@ def check_gpu_memory(threshold=0.9):
     """
     if not torch.cuda.is_available():
         return False
-    device = torch.cuda.current_device()
+    device = torch.npu.current_device()
     allocated = torch.cuda.memory_allocated(device)
     reserved = torch.cuda.memory_reserved(device)
     total = torch.cuda.get_device_properties(device).total_memory
diff --git a/megatron/core/distributed/fsdp/src/megatron_fsdp/utils.py b/megatron/core/distributed/fsdp/src/megatron_fsdp/utils.py
old mode 100644
new mode 100755
index d5fbc91..793ca48
--- a/megatron/core/distributed/fsdp/src/megatron_fsdp/utils.py
+++ b/megatron/core/distributed/fsdp/src/megatron_fsdp/utils.py
@@ -182,7 +182,7 @@ def _get_cuda_rng_state(
         device = torch.device("cuda", device)
     idx = device.index
     if idx is None:
-        idx = torch.cuda.current_device()
+        idx = torch.npu.current_device()
 
     default_generator = torch.cuda.default_generators[idx]
     if clone:
@@ -220,7 +220,7 @@ def _set_cuda_rng_state(new_state: torch.Tensor, device: int = -1, graph_safe: b
         def cb():
             idx = device.index
             if idx is None:
-                idx = torch.cuda.current_device()
+                idx = torch.npu.current_device()
             default_generator = torch.cuda.default_generators[idx]
 
             # if graph capturing, set the rng state in a cudagraphable way
@@ -359,10 +359,10 @@ def initialize_rng_tracker(
                     self.states_[name] = new_state
                 else:
                     # Get the current rng state.
-                    orig_rng_state = torch.cuda.get_rng_state()
+                    orig_rng_state = torch.npu.get_rng_state()
                     # Set the new state and store it.
-                    torch.cuda.manual_seed(seed)
-                    self.states_[name] = torch.cuda.get_rng_state()
+                    torch.npu.manual_seed(seed)
+                    self.states_[name] = torch.npu.get_rng_state()
                     # Reset rng state to what it was.
                     _set_cuda_rng_state(orig_rng_state)
 
@@ -717,7 +717,7 @@ class GlobalMemoryBuffer:
                 self.buffer[(name, dtype)] = torch.empty(
                     required_len,
                     dtype=dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     requires_grad=False,
                 )
 
diff --git a/megatron/core/distributed/param_and_grad_buffer.py b/megatron/core/distributed/param_and_grad_buffer.py
old mode 100644
new mode 100755
index b192f18..3c20cfd
--- a/megatron/core/distributed/param_and_grad_buffer.py
+++ b/megatron/core/distributed/param_and_grad_buffer.py
@@ -780,7 +780,7 @@ class _ParamAndGradBuffer:
                 self.shared_buffer = torch.zeros(
                     self.numel,
                     dtype=self.grad_dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     requires_grad=False,
                 )
                 # For FP32 weight grads, only half of the buffer is used to store params in bf16.
@@ -797,13 +797,13 @@ class _ParamAndGradBuffer:
                     self.param_data = torch.zeros(
                         self.numel,
                         dtype=self.param_dtype,
-                        device=torch.cuda.current_device(),
+                        device=torch.npu.current_device(),
                         requires_grad=False,
                     )
                 self.grad_data = torch.zeros(
                     self.numel,
                     dtype=self.grad_dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     requires_grad=False,
                 )
 
diff --git a/megatron/core/energy_monitor.py b/megatron/core/energy_monitor.py
old mode 100644
new mode 100755
index 4334cfe..d532a8d
--- a/megatron/core/energy_monitor.py
+++ b/megatron/core/energy_monitor.py
@@ -38,7 +38,7 @@ class EnergyMonitor:
         """Setup the NVML Handler."""
         if has_nvml:
             nvmlInit()
-            self._handle = nvmlDeviceGetHandleByIndex(torch.cuda.current_device())
+            self._handle = nvmlDeviceGetHandleByIndex(torch.npu.current_device())
 
     def shutdown(self) -> None:
         """Shutdown NVML."""
diff --git a/megatron/core/export/trtllm/trtllm_weights_converter/distributed_trtllm_model_weights_converter.py b/megatron/core/export/trtllm/trtllm_weights_converter/distributed_trtllm_model_weights_converter.py
old mode 100644
new mode 100755
index b67a6dc..682eb40
--- a/megatron/core/export/trtllm/trtllm_weights_converter/distributed_trtllm_model_weights_converter.py
+++ b/megatron/core/export/trtllm/trtllm_weights_converter/distributed_trtllm_model_weights_converter.py
@@ -219,7 +219,7 @@ class DistributedTRTLLMModelWeightsConverter:
             dim_size = list(val.size())
             dim_size[0] = vocab_size_padded
             gathered_val = torch.zeros(
-                dim_size, dtype=val.dtype, device=torch.cuda.current_device()
+                dim_size, dtype=val.dtype, device=torch.npu.current_device()
             )
             gathered_val[vocab_start_index:vocab_end_index] = val
             torch.distributed.all_reduce(gathered_val, group=self.tp_group)
diff --git a/megatron/core/extensions/transformer_engine.py b/megatron/core/extensions/transformer_engine.py
old mode 100644
new mode 100755
index ef8527e..0477ed4
--- a/megatron/core/extensions/transformer_engine.py
+++ b/megatron/core/extensions/transformer_engine.py
@@ -302,7 +302,7 @@ def _get_extra_te_kwargs(config: TransformerConfig):
         elif config.init_model_with_meta_device:
             extra_transformer_engine_kwargs["device"] = "meta"
         else:
-            extra_transformer_engine_kwargs["device"] = torch.cuda.current_device()
+            extra_transformer_engine_kwargs["device"] = torch.npu.current_device()
     return extra_transformer_engine_kwargs
 
 
diff --git a/megatron/core/inference/batch_dimensions_utils.py b/megatron/core/inference/batch_dimensions_utils.py
old mode 100644
new mode 100755
index e6ea32a..8418740
--- a/megatron/core/inference/batch_dimensions_utils.py
+++ b/megatron/core/inference/batch_dimensions_utils.py
@@ -185,7 +185,7 @@ class InferenceBatchDimensions:
                 int(has_explicit_chunked_prefill_req),
             ],
             dtype=torch.int32,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
 
         torch.distributed.all_reduce(sync_tensor, op=torch.distributed.ReduceOp.MAX, group=ep_group)
diff --git a/megatron/core/inference/communication_utils.py b/megatron/core/inference/communication_utils.py
old mode 100644
new mode 100755
index a5bfe75..da4a59f
--- a/megatron/core/inference/communication_utils.py
+++ b/megatron/core/inference/communication_utils.py
@@ -73,7 +73,7 @@ def broadcast_from_last_pipeline_stage(
         assert dtype == tensor.dtype, f"Expected tensor of type {dtype} but got {tensor.dtype}"
         _is_cuda_contiguous(tensor)
     else:
-        tensor = torch.empty(size, dtype=dtype, device=torch.cuda.current_device())
+        tensor = torch.empty(size, dtype=dtype, device=torch.npu.current_device())
 
     # Broadcast the tensor
     torch.distributed.broadcast(tensor, src=last_rank, group=pp_group)
@@ -155,7 +155,7 @@ def broadcast_tensor(size, dtype, tensor=None, rank=0, data_parallel=False):
     if torch.distributed.get_rank() == rank:
         _is_cuda_contiguous(tensor)
     else:
-        tensor = torch.empty(size, dtype=dtype, device=torch.cuda.current_device())
+        tensor = torch.empty(size, dtype=dtype, device=torch.npu.current_device())
 
     group = None
     if data_parallel:
@@ -177,12 +177,12 @@ def broadcast_list(size, dtype, list_values=None, rank=0, data_parallel=False):
 
     if data_parallel:
         if parallel_state.get_model_parallel_src_rank() == torch.distributed.get_rank():
-            tensor = torch.tensor(list_values, dtype=dtype, device=torch.cuda.current_device())
+            tensor = torch.tensor(list_values, dtype=dtype, device=torch.npu.current_device())
 
         rank = parallel_state.get_model_parallel_src_rank()
     else:
         if torch.distributed.get_rank() == rank:
-            tensor = torch.tensor(list_values, dtype=dtype, device=torch.cuda.current_device())
+            tensor = torch.tensor(list_values, dtype=dtype, device=torch.npu.current_device())
 
     return broadcast_tensor(size, dtype, tensor=tensor, rank=rank, data_parallel=data_parallel)
 
diff --git a/megatron/core/inference/contexts/attention_context/mamba_metadata.py b/megatron/core/inference/contexts/attention_context/mamba_metadata.py
old mode 100644
new mode 100755
index 6cf45ae..508dbfd
--- a/megatron/core/inference/contexts/attention_context/mamba_metadata.py
+++ b/megatron/core/inference/contexts/attention_context/mamba_metadata.py
@@ -43,11 +43,11 @@ class MambaMetadata:
         """
         self.max_requests = max_requests
         self.max_tokens = max_tokens
-        self.device = torch.cuda.current_device()
+        self.device = torch.npu.current_device()
 
         # Map from requests to slots in the static Mamba state buffer
         self.request_to_mamba_state_idx = torch.full(
-            (self.max_requests,), -1, dtype=torch.int32, device=torch.cuda.current_device()
+            (self.max_requests,), -1, dtype=torch.int32, device=torch.npu.current_device()
         )
 
         # Map from requests to slots in the static Mamba state buffer for active decode requests
@@ -90,7 +90,7 @@ class MambaMetadata:
 
         # Allocator for Mamba state slots
         self.mamba_state_free_slots = torch.arange(
-            self.max_requests, dtype=torch.int32, device=torch.cuda.current_device()
+            self.max_requests, dtype=torch.int32, device=torch.npu.current_device()
         )
         self.mamba_state_free_slot_count = self.max_requests
 
@@ -104,7 +104,7 @@ class MambaMetadata:
 
         # Re-initialize the free slot pool
         self.mamba_state_free_slots = torch.arange(
-            self.max_requests, dtype=torch.int32, device=torch.cuda.current_device()
+            self.max_requests, dtype=torch.int32, device=torch.npu.current_device()
         )
         self.mamba_state_free_slot_count = self.max_requests
 
diff --git a/megatron/core/inference/contexts/attention_context/mha_metadata.py b/megatron/core/inference/contexts/attention_context/mha_metadata.py
old mode 100644
new mode 100755
index 1b6e802..e2f4839
--- a/megatron/core/inference/contexts/attention_context/mha_metadata.py
+++ b/megatron/core/inference/contexts/attention_context/mha_metadata.py
@@ -15,7 +15,7 @@ class MHAMetadata(MetadataBase):
         self, block_count_total, max_kv_block_count, max_requests, block_size_tokens, max_seqlen
     ):
         super().__init__()
-        device = torch.cuda.current_device()
+        device = torch.npu.current_device()
         self.device = device
         self.max_blocks = block_count_total
         self.max_kv_blocks = max_kv_block_count
diff --git a/megatron/core/inference/contexts/dynamic_block_allocator.py b/megatron/core/inference/contexts/dynamic_block_allocator.py
old mode 100644
new mode 100755
index 8207a17..6f667a5
--- a/megatron/core/inference/contexts/dynamic_block_allocator.py
+++ b/megatron/core/inference/contexts/dynamic_block_allocator.py
@@ -34,7 +34,7 @@ class BlockAllocator:
 
         # Initialize block pool as a "stack" data structure
         self.block_bag = torch.arange(
-            self.total_count, dtype=torch.int32, device=torch.cuda.current_device()
+            self.total_count, dtype=torch.int32, device=torch.npu.current_device()
         )
 
     def __str__(self):
@@ -129,7 +129,7 @@ class BlockAllocator:
         # requests will point to each other's memory blocks, resulting in faulty
         # generations.
         self.block_bag = torch.arange(
-            self.total_count, dtype=torch.int32, device=torch.cuda.current_device()
+            self.total_count, dtype=torch.int32, device=torch.npu.current_device()
         )
 
         self.total_avail = self.total_count - 1
diff --git a/megatron/core/inference/contexts/dynamic_context.py b/megatron/core/inference/contexts/dynamic_context.py
old mode 100644
new mode 100755
index 5dc2d50..3803f6d
--- a/megatron/core/inference/contexts/dynamic_context.py
+++ b/megatron/core/inference/contexts/dynamic_context.py
@@ -453,7 +453,7 @@ class DynamicInferenceContext(BaseInferenceContext):
         # (i.e., divergence in the scheduling behavior).
         if pp_size > 1:
             block_count_tensor = torch.tensor(
-                block_count, dtype=torch.int32, device=torch.cuda.current_device()
+                block_count, dtype=torch.int32, device=torch.npu.current_device()
             )
             torch.distributed.all_reduce(
                 block_count_tensor,
@@ -616,7 +616,7 @@ class DynamicInferenceContext(BaseInferenceContext):
 
         # Per-request state.
         self.request_ids = torch.full(
-            (self.max_requests,), -1, dtype=torch.int32, device=torch.cuda.current_device()
+            (self.max_requests,), -1, dtype=torch.int32, device=torch.npu.current_device()
         )
         # request_query_lengths is the input prompt tokens length during prefill phase (1st step) and then 1 for the decode phase (i.e During generation)
         self.request_query_lengths = torch.empty_like(self.request_ids)
@@ -632,20 +632,20 @@ class DynamicInferenceContext(BaseInferenceContext):
             (self.max_requests, self.max_kv_block_count),
             -1,
             dtype=torch.int,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
 
         # Track request metadata.
         self.request_metadata = {
             label: torch.empty(
-                (self.max_requests,), dtype=dtype, device=torch.cuda.current_device()
+                (self.max_requests,), dtype=dtype, device=torch.npu.current_device()
             )
             for label, dtype, _ in self.request_metadata_types
         }
 
         # Per-token state.
         self.token_to_input_ids = torch.full(
-            (self.max_tokens,), 0, dtype=torch.long, device=torch.cuda.current_device()
+            (self.max_tokens,), 0, dtype=torch.long, device=torch.npu.current_device()
         )
         self.token_to_pos_ids = torch.full_like(self.token_to_input_ids, 0)
         self.token_to_request_idx = torch.empty_like(self.token_to_input_ids)
@@ -669,7 +669,7 @@ class DynamicInferenceContext(BaseInferenceContext):
                         self.kv_reduced_dim,
                     ),
                     dtype=self.params_dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 )
             else:
                 ctx = (
@@ -689,7 +689,7 @@ class DynamicInferenceContext(BaseInferenceContext):
                             self.hidden_size_per_attention_head,
                         ),
                         dtype=self.params_dtype,
-                        device=torch.cuda.current_device(),
+                        device=torch.npu.current_device(),
                     )
 
         # Optional state tensors for hybrid models
@@ -703,12 +703,12 @@ class DynamicInferenceContext(BaseInferenceContext):
                 self.mamba_conv_states = torch.empty(
                     (self.num_mamba_layers, self.max_requests) + self.mamba_conv_states_shape,
                     dtype=self.params_dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 )
                 self.mamba_ssm_states = torch.empty(
                     (self.num_mamba_layers, self.max_requests) + self.mamba_ssm_states_shape,
                     dtype=self.params_dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 )
 
             else:
@@ -1185,7 +1185,7 @@ class DynamicInferenceContext(BaseInferenceContext):
         self.request_kv_block_counts[request_slice] = block_counts
         for i, (label, dtype, _) in enumerate(self.request_metadata_types):
             self.request_metadata[label][request_slice] = torch.tensor(
-                metadata_cols[i], dtype=dtype, device=torch.cuda.current_device()
+                metadata_cols[i], dtype=dtype, device=torch.npu.current_device()
             )
 
         dummy_block_idx = self.block_allocator.dummy_block_idx
@@ -1264,7 +1264,7 @@ class DynamicInferenceContext(BaseInferenceContext):
 
         # Pre-construct shared objects (safe due to deep copy in DynamicInferenceRequest.__post_init__)
         shared_sampling_params = SamplingParams(num_tokens_to_generate=1, termination_id=-1)
-        shared_decode_tokens = torch.zeros(1, dtype=torch.long, device=torch.cuda.current_device())
+        shared_decode_tokens = torch.zeros(1, dtype=torch.long, device=torch.npu.current_device())
 
         decode_requests = [
             DynamicInferenceRequest(
@@ -1294,7 +1294,7 @@ class DynamicInferenceContext(BaseInferenceContext):
         # Create a single large tensor and slice from it for each prefill request
         max_prefill_tokens = per_prefill_tokens + (1 if rem_prefill_tokens > 0 else 0)
         shared_prefill_tokens = torch.zeros(
-            max_prefill_tokens, dtype=torch.long, device=torch.cuda.current_device()
+            max_prefill_tokens, dtype=torch.long, device=torch.npu.current_device()
         )
 
         prefill_requests = [
@@ -1838,7 +1838,7 @@ class DynamicInferenceContext(BaseInferenceContext):
             row_idx = torch.arange(
                 self.paused_request_count,
                 self.paused_request_count + resume_request_count,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
             col_idx = self.request_kv_block_counts[
                 self.paused_request_count : (self.paused_request_count + resume_request_count)
@@ -1906,7 +1906,7 @@ class DynamicInferenceContext(BaseInferenceContext):
             -1,
             -1,
             dtype=paused_block_counts_cumsum.dtype,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         net_block_counts = paused_block_counts_cumsum - remaining_paused_request_counts
         evict_request_count = torch.nonzero(net_block_counts >= 0)[0].item() + 1
@@ -1915,7 +1915,7 @@ class DynamicInferenceContext(BaseInferenceContext):
         evict_start_idx = self.paused_request_count - evict_request_count
         evict_end_idx = self.paused_request_count
         evict_request_idxs = torch.arange(
-            evict_start_idx, evict_end_idx, device=torch.cuda.current_device()
+            evict_start_idx, evict_end_idx, device=torch.npu.current_device()
         )
         evict_request_ids = self.request_ids[evict_start_idx:evict_end_idx].clone()
 
@@ -1929,24 +1929,24 @@ class DynamicInferenceContext(BaseInferenceContext):
             src_idxs = torch.arange(
                 self.paused_request_count - evict_request_count,
                 self.paused_request_count,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
             dst_idxs = torch.arange(
                 self.total_request_count - evict_request_count,
                 self.total_request_count,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         else:
             # Swap all active requests with left-most evicted requests.
             src_idxs = torch.arange(
                 self.paused_request_count - evict_request_count,
                 self.paused_request_count - evict_request_count + active_request_count,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
             dst_idxs = torch.arange(
                 self.paused_request_count,
                 self.paused_request_count + active_request_count,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
 
         # Swap evicted and active requests.
@@ -2212,7 +2212,7 @@ class DynamicInferenceContext(BaseInferenceContext):
 
         # 8. We make relevant changes to the token bookkeeping tensors
         self.token_to_request_idx[: self.active_token_count] = torch.arange(
-            self.paused_request_count, self.total_request_count, device=torch.cuda.current_device()
+            self.paused_request_count, self.total_request_count, device=torch.npu.current_device()
         )
         self.token_to_position_in_request[: self.active_token_count] = (
             self.request_kv_length_offsets[self.paused_request_count : self.total_request_count]
diff --git a/megatron/core/inference/engines/dynamic_engine.py b/megatron/core/inference/engines/dynamic_engine.py
old mode 100644
new mode 100755
index 0a95e8f..043a892
--- a/megatron/core/inference/engines/dynamic_engine.py
+++ b/megatron/core/inference/engines/dynamic_engine.py
@@ -811,16 +811,16 @@ class DynamicInferenceEngine(AbstractEngine):
             except TypeError:
                 prompt_token_ids = self.controller.tokenize_prompt(prompt)
             tokens = torch.tensor(
-                prompt_token_ids, dtype=torch.int64, device=torch.cuda.current_device()
+                prompt_token_ids, dtype=torch.int64, device=torch.npu.current_device()
             )
         elif isinstance(prompt, list):
             # Convert List[int] -> Tensor.
-            tokens = torch.tensor(prompt, dtype=torch.int64, device=torch.cuda.current_device())
+            tokens = torch.tensor(prompt, dtype=torch.int64, device=torch.npu.current_device())
         elif isinstance(prompt, torch.Tensor):
             # Prompt already tokenized.
             assert prompt.dtype == torch.int64, prompt.dtype
             assert prompt.device == torch.device(
-                f"cuda:{torch.cuda.current_device()}"
+                f"cuda:{torch.npu.current_device()}"
             ), prompt.device
             tokens = prompt
 
diff --git a/megatron/core/inference/engines/static_engine.py b/megatron/core/inference/engines/static_engine.py
old mode 100644
new mode 100755
index d4c6196..cff9eae
--- a/megatron/core/inference/engines/static_engine.py
+++ b/megatron/core/inference/engines/static_engine.py
@@ -390,4 +390,4 @@ class StaticInferenceEngine(AbstractEngine):
         """Runs the engine asynchronously using asyncio"""
         loop = get_asyncio_loop(loop)
 
-        await loop.run_in_executor(None, self._wrapped_run_engine, torch.cuda.current_device())
+        await loop.run_in_executor(None, self._wrapped_run_engine, torch.npu.current_device())
diff --git a/megatron/core/inference/model_inference_wrappers/abstract_model_inference_wrapper.py b/megatron/core/inference/model_inference_wrappers/abstract_model_inference_wrapper.py
old mode 100644
new mode 100755
index 6a17de6..489e55b
--- a/megatron/core/inference/model_inference_wrappers/abstract_model_inference_wrapper.py
+++ b/megatron/core/inference/model_inference_wrappers/abstract_model_inference_wrapper.py
@@ -171,10 +171,10 @@ class AbstractModelInferenceWrapper(abc.ABC):
         # so that the dummy forward pass will work with sequence parallel
         num_dummy_tokens = self.tp_size
         tokens = torch.zeros(
-            (1, num_dummy_tokens), dtype=torch.long, device=torch.cuda.current_device()
+            (1, num_dummy_tokens), dtype=torch.long, device=torch.npu.current_device()
         )
         position_ids = torch.zeros(
-            (1, num_dummy_tokens), dtype=torch.long, device=torch.cuda.current_device()
+            (1, num_dummy_tokens), dtype=torch.long, device=torch.npu.current_device()
         )
         attention_mask = None
         return self.model(tokens, position_ids, attention_mask)
@@ -206,7 +206,7 @@ class AbstractModelInferenceWrapper(abc.ABC):
             seq_len = seq_len // self.tp_size
         recv_size = (seq_len, batch_size, self.inference_wrapper_config.hidden_size)
         return torch.empty(
-            recv_size, dtype=self.pipeline_communication_dtype, device=torch.cuda.current_device()
+            recv_size, dtype=self.pipeline_communication_dtype, device=torch.npu.current_device()
         )
 
     def forward_pass_without_pipeline_parallel(
@@ -310,7 +310,7 @@ class AbstractModelInferenceWrapper(abc.ABC):
             logits = torch.empty(
                 (batch_size, logits_seq_len, self.inference_wrapper_config.padded_vocab_size),
                 dtype=self.pipeline_communication_dtype,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
 
         recv_buffer = None
diff --git a/megatron/core/inference/text_generation_controllers/text_generation_controller.py b/megatron/core/inference/text_generation_controllers/text_generation_controller.py
old mode 100644
new mode 100755
index a523398..3e4044a
--- a/megatron/core/inference/text_generation_controllers/text_generation_controller.py
+++ b/megatron/core/inference/text_generation_controllers/text_generation_controller.py
@@ -72,7 +72,7 @@ class TextGenerationController:
         )
 
         model_config = get_model_config(self.inference_wrapped_model.model)
-        self.sampling_rng = torch.Generator(device=torch.cuda.current_device())
+        self.sampling_rng = torch.Generator(device=torch.npu.current_device())
         self.sampling_rng.manual_seed(model_config.inference_sampling_seed)
 
         if self.inference_wrapped_model.inference_context.is_dynamic_batching():
@@ -97,7 +97,7 @@ class TextGenerationController:
         # Callback to get request IDs that should be marked as finished due to stop words
         self._get_stop_word_finished_ids_callback = None
 
-        device = torch.cuda.current_device()
+        device = torch.npu.current_device()
         logits_dtype = self.inference_wrapped_model.inference_wrapper_config.params_dtype
         # Use padded vocab size because tokenizer vocab size might pad to nearest power of 2.
         vocab_size = self.inference_wrapped_model.inference_wrapper_config.padded_vocab_size
@@ -473,7 +473,7 @@ class TextGenerationController:
             [self.tokenizer.eod] * padded_sequence_length for _ in range(num_padded_requests)
         ]
 
-        tokens = torch.tensor(padded_prompt_tokens_list, device=torch.cuda.current_device())
+        tokens = torch.tensor(padded_prompt_tokens_list, device=torch.npu.current_device())
 
         return tokens
 
@@ -996,7 +996,7 @@ class TextGenerationController:
         )
         prompt_lengths_in_batch = torch.tensor(
             [len(prompt_tokens) for prompt_tokens in batch_prompt_tokens_list],
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         max_prompt_length_in_batch = max(prompt_lengths_in_batch)
         min_prompt_length_in_batch = min(prompt_lengths_in_batch)
@@ -1053,17 +1053,17 @@ class TextGenerationController:
             output_log_probs = torch.empty(
                 (batch_size, max_sequence_length - 1),
                 dtype=torch.float32,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
 
         # An array to check which of the prompts have reached end of generation condition
         is_generation_done_tensor = torch.zeros(
-            batch_size, dtype=torch.bool, device=torch.cuda.current_device()
+            batch_size, dtype=torch.bool, device=torch.npu.current_device()
         )
 
         # An array to act as a counter to keep track of generated sequence lengths
         generated_sequence_lengths = torch.zeros(
-            batch_size, device=torch.cuda.current_device()
+            batch_size, device=torch.npu.current_device()
         ).cuda()
 
         # Use padded vocab size because tokenizer vocab size might not include padding
diff --git a/megatron/core/inference/text_generation_server/dynamic_text_gen_server/endpoints/common.py b/megatron/core/inference/text_generation_server/dynamic_text_gen_server/endpoints/common.py
old mode 100644
new mode 100755
index 6efdba5..cfbba20
--- a/megatron/core/inference/text_generation_server/dynamic_text_gen_server/endpoints/common.py
+++ b/megatron/core/inference/text_generation_server/dynamic_text_gen_server/endpoints/common.py
@@ -10,5 +10,5 @@ LOCK = threading.Lock()
 
 def send_do_generate():
     """Broadcasts a message to perform a generation to all tensor parallel ranks."""
-    choice = torch.tensor([GENERATE_NUM], dtype=torch.long, device=torch.cuda.current_device())
+    choice = torch.tensor([GENERATE_NUM], dtype=torch.long, device=torch.npu.current_device())
     torch.distributed.broadcast(choice, 0)
diff --git a/megatron/core/inference/text_generation_server/endpoints/common.py b/megatron/core/inference/text_generation_server/endpoints/common.py
old mode 100644
new mode 100755
index 6efdba5..cfbba20
--- a/megatron/core/inference/text_generation_server/endpoints/common.py
+++ b/megatron/core/inference/text_generation_server/endpoints/common.py
@@ -10,5 +10,5 @@ LOCK = threading.Lock()
 
 def send_do_generate():
     """Broadcasts a message to perform a generation to all tensor parallel ranks."""
-    choice = torch.tensor([GENERATE_NUM], dtype=torch.long, device=torch.cuda.current_device())
+    choice = torch.tensor([GENERATE_NUM], dtype=torch.long, device=torch.npu.current_device())
     torch.distributed.broadcast(choice, 0)
diff --git a/megatron/core/inference/unified_memory.py b/megatron/core/inference/unified_memory.py
old mode 100644
new mode 100755
index 6b58e84..8e48101
--- a/megatron/core/inference/unified_memory.py
+++ b/megatron/core/inference/unified_memory.py
@@ -239,10 +239,10 @@ def compile_allocator():
         # Synchronize failure state across ranks. (For currently unknown reasons,
         # one rank can show as FAILURE while the remaining ranks show as SUCCESS.)
         local_state = torch.tensor(
-            [_compilation_state.value], dtype=torch.uint8, device=torch.cuda.current_device()
+            [_compilation_state.value], dtype=torch.uint8, device=torch.npu.current_device()
         )
         world_states = [
-            torch.empty(1, dtype=torch.uint8, device=torch.cuda.current_device())
+            torch.empty(1, dtype=torch.uint8, device=torch.npu.current_device())
             for _ in range(torch.distributed.get_world_size())
         ]
         torch.distributed.all_gather(world_states, local_state)
diff --git a/megatron/core/inference/utils.py b/megatron/core/inference/utils.py
old mode 100644
new mode 100755
index 0bdaff6..cb1f892
--- a/megatron/core/inference/utils.py
+++ b/megatron/core/inference/utils.py
@@ -32,7 +32,7 @@ class Counter:
 def get_attention_mask(seq_length: int) -> torch.Tensor:
     """Constructs an attention mask given the input sequence length."""
     attention_mask = torch.tril(
-        torch.ones((1, seq_length, seq_length), device=torch.cuda.current_device())
+        torch.ones((1, seq_length, seq_length), device=torch.npu.current_device())
     ).view(1, 1, seq_length, seq_length)
 
     # Convert to boolean
diff --git a/megatron/core/jit.py b/megatron/core/jit.py
old mode 100644
new mode 100755
index b67810f..10335d1
--- a/megatron/core/jit.py
+++ b/megatron/core/jit.py
@@ -5,27 +5,28 @@ import torch
 from megatron.core.utils import is_torch_min_version
 
 jit_fuser = torch.jit.script
-# nvFuser is deprecated in PyTorch JIT starting from 2.2
 
 
 def noop_decorator(func):
-    '''No-op decorator'''
+    """No-op decorator"""
     return func
 
 
 def enable_jit_fuser():
-    '''Enable the JIT fuser'''
+    """Enable the JIT fuser"""
     global jit_fuser
     try:
+        if torch.npu.is_available():
+            jit_fuser = noop_decorator
+            return
         if is_torch_min_version("2.2.0a0"):
             jit_fuser = torch.compile
     except ImportError:
-
         jit_fuser = noop_decorator
 
 
 def disable_jit_fuser():
-    '''Disable the JIT fuser'''
+    """Disable the JIT fuser"""
     global jit_fuser
     jit_fuser = noop_decorator
 
diff --git a/megatron/core/models/bert/bert_model.py b/megatron/core/models/bert/bert_model.py
old mode 100644
new mode 100755
index abe9bc1..da21f77
--- a/megatron/core/models/bert/bert_model.py
+++ b/megatron/core/models/bert/bert_model.py
@@ -361,7 +361,7 @@ class BertModel(LanguageModule):
             output = torch.zeros(
                 size=(embeddings.shape[0], embeddings.shape[2]),
                 dtype=torch.float32,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
             for i, (embedding, mask) in enumerate(zip(embeddings, masks)):
                 output[i, :] = torch.mean(embedding[1 : mask - 1], dim=0)
diff --git a/megatron/core/models/common/embeddings/rotary_pos_embedding.py b/megatron/core/models/common/embeddings/rotary_pos_embedding.py
old mode 100644
new mode 100755
index 05a7e8f..418670b
--- a/megatron/core/models/common/embeddings/rotary_pos_embedding.py
+++ b/megatron/core/models/common/embeddings/rotary_pos_embedding.py
@@ -75,7 +75,7 @@ class RotaryEmbedding(nn.Module):
         self.rotary_interleaved = rotary_interleaved
 
         self.seq_len_interpolation_factor = seq_len_interpolation_factor
-        device = 'cpu' if use_cpu_initialization else torch.cuda.current_device()
+        device = 'cpu' if use_cpu_initialization else torch.npu.current_device()
         self.inv_freq = 1.0 / (
             rotary_base ** (torch.arange(0, dim, 2, dtype=torch.float32, device=device) / dim)
         )
@@ -159,7 +159,7 @@ class RotaryEmbedding(nn.Module):
         """
         if self.inv_freq.device.type == 'cpu':
             # move `inv_freq` to GPU once at the first micro-batch forward pass
-            self.inv_freq = self.inv_freq.to(device=torch.cuda.current_device())
+            self.inv_freq = self.inv_freq.to(device=torch.npu.current_device())
 
         freqs = self.get_freqs_non_repeated(max_seq_len, offset)
         # first part even vector components, second part odd vector components,
@@ -294,7 +294,7 @@ class MultimodalRotaryEmbedding(nn.Module):
         self.inv_freq = 1.0 / (
             rotary_base
             ** (
-                torch.arange(0, dim, 2, dtype=torch.float32, device=torch.cuda.current_device())
+                torch.arange(0, dim, 2, dtype=torch.float32, device=torch.npu.current_device())
                 / dim
             )
         )
diff --git a/megatron/core/models/common/embeddings/yarn_rotary_pos_embedding.py b/megatron/core/models/common/embeddings/yarn_rotary_pos_embedding.py
old mode 100644
new mode 100755
index 166ef9b..531504e
--- a/megatron/core/models/common/embeddings/yarn_rotary_pos_embedding.py
+++ b/megatron/core/models/common/embeddings/yarn_rotary_pos_embedding.py
@@ -73,7 +73,7 @@ class YarnRotaryEmbedding(RotaryEmbedding):
         self.mscale_all_dim = mscale_all_dim
         self.correction_range_round_to_int = correction_range_round_to_int
 
-        device = 'cpu' if use_cpu_initialization else torch.cuda.current_device()
+        device = 'cpu' if use_cpu_initialization else torch.npu.current_device()
 
         with torch.device(device):
             self.inv_freq_extra = 1.0 / (
@@ -119,11 +119,11 @@ class YarnRotaryEmbedding(RotaryEmbedding):
 
         if self.inv_freq_extra.device.type == 'cpu':
             # move `inv_freq_extra` to GPU once at the first micro-batch forward pass
-            self.inv_freq_extra = self.inv_freq_extra.to(device=torch.cuda.current_device())
+            self.inv_freq_extra = self.inv_freq_extra.to(device=torch.npu.current_device())
 
         if self.inv_freq_inter.device.type == 'cpu':
             # move `inv_freq_inter` to GPU once at the first micro-batch forward pass
-            self.inv_freq_inter = self.inv_freq_inter.to(device=torch.cuda.current_device())
+            self.inv_freq_inter = self.inv_freq_inter.to(device=torch.npu.current_device())
 
         low, high = _yarn_find_correction_range(
             self.beta_fast,
diff --git a/megatron/core/models/gpt/gpt_layer_specs.py b/megatron/core/models/gpt/gpt_layer_specs.py
index 49501ee..0dc63fc 100755
--- a/megatron/core/models/gpt/gpt_layer_specs.py
+++ b/megatron/core/models/gpt/gpt_layer_specs.py
@@ -48,6 +48,7 @@ try:
     HAVE_TE = True
 except ImportError:
     HAVE_TE = False
+    TESpecProvider = LocalSpecProvider
 
 try:
     from megatron.core.extensions.kitchen import HAVE_KITCHEN, KitchenSpecProvider
diff --git a/megatron/core/models/gpt/gpt_model.py b/megatron/core/models/gpt/gpt_model.py
old mode 100644
new mode 100755
index e287344..5ad7470
--- a/megatron/core/models/gpt/gpt_model.py
+++ b/megatron/core/models/gpt/gpt_model.py
@@ -412,7 +412,7 @@ class GPTModel(LanguageModule):
             sequence_len_offset = torch.tensor(
                 [inference_context.sequence_len_offset] * current_batch_size,
                 dtype=torch.int32,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         else:
             sequence_len_offset = None
diff --git a/megatron/core/nccl_allocator.py b/megatron/core/nccl_allocator.py
old mode 100644
new mode 100755
index d475684..fb464db
--- a/megatron/core/nccl_allocator.py
+++ b/megatron/core/nccl_allocator.py
@@ -165,7 +165,7 @@ def register_mem_pool(pool, group, symmetric=True):
     Register a memory pool to a group.
     symmetric: bool, this is for future use.
     """
-    backend = group._get_backend(torch.device("cuda", torch.cuda.current_device()))
+    backend = group._get_backend(torch.device("cuda", torch.npu.current_device()))
     if symmetric:
         try:
             backend.register_mem_pool(pool, symm=symmetric)
@@ -186,7 +186,7 @@ def deregister_mem_pool(pool, group):
     """
     Deregister a memory pool from a group.
     """
-    backend = group._get_backend(torch.device("cuda", torch.cuda.current_device()))
+    backend = group._get_backend(torch.device("cuda", torch.npu.current_device()))
     if pool.snapshot():
         backend.deregister_mem_pool(pool)
 
@@ -206,7 +206,7 @@ class nccl_mem:
 
         if enabled:
             if device is None:
-                self.device = torch.device("cuda", torch.cuda.current_device())
+                self.device = torch.device("cuda", torch.npu.current_device())
             elif isinstance(device, int):
                 self.device = torch.device("cuda", device)
             elif isinstance(device, str):
@@ -317,7 +317,7 @@ class MultiGroupMemPoolAllocator:
         # If the pool is not empty, deregister the pool from all the groups.
         if self.pool.snapshot():
             for group in self.groups:
-                backend = group._get_backend(torch.device("cuda", torch.cuda.current_device()))
+                backend = group._get_backend(torch.device("cuda", torch.npu.current_device()))
                 try:
                     # Since the registration is done in mempool granularity, we need to deregister
                     # the tensors in the mempool and re-register the mempool including
@@ -334,7 +334,7 @@ class MultiGroupMemPoolAllocator:
 
     def __exit__(self, *args):
         for group in self.groups:
-            backend = group._get_backend(torch.device("cuda", torch.cuda.current_device()))
+            backend = group._get_backend(torch.device("cuda", torch.npu.current_device()))
             try:
                 # Prefer attempting symmetric registration first; fall back if unsupported.
                 if self.symmetric:
diff --git a/megatron/core/optimizer/distrib_optimizer.py b/megatron/core/optimizer/distrib_optimizer.py
old mode 100644
new mode 100755
index 4192b0b..fbfe00e
--- a/megatron/core/optimizer/distrib_optimizer.py
+++ b/megatron/core/optimizer/distrib_optimizer.py
@@ -788,7 +788,7 @@ class DistributedOptimizer(MixedPrecisionOptimizer):
                             # Allocate dummy tensors.
                             numel = len(param_range_map["gbuf_world"])
                             init_shard = lambda dtype=torch.float32: torch.empty(
-                                (numel,), dtype=dtype, device=torch.cuda.current_device()
+                                (numel,), dtype=dtype, device=torch.npu.current_device()
                             )
 
                             # For precision_aware_optimizer, the empty tensors should also be
@@ -1074,7 +1074,7 @@ class DistributedOptimizer(MixedPrecisionOptimizer):
 
                             # Gather tensor list.
                             if data_parallel_rank == 0 or return_on_all_ranks:
-                                device = "cpu" if use_gloo_comm else torch.cuda.current_device()
+                                device = "cpu" if use_gloo_comm else torch.npu.current_device()
                                 recv_tensors = [
                                     torch.zeros(
                                         (gbuf_local_numel,), dtype=torch.float32, device=device
diff --git a/megatron/core/optimizer/grad_scaler.py b/megatron/core/optimizer/grad_scaler.py
old mode 100644
new mode 100755
index abdd1e7..d19122d
--- a/megatron/core/optimizer/grad_scaler.py
+++ b/megatron/core/optimizer/grad_scaler.py
@@ -137,6 +137,6 @@ class DynamicGradScaler(MegatronGradScaler):
         return state_dict
 
     def load_state_dict(self, state_dict: Dict):
-        self._scale = state_dict['scale'].cuda(torch.cuda.current_device())
+        self._scale = state_dict['scale'].cuda(torch.npu.current_device())
         self._growth_tracker = state_dict['growth_tracker']
         self._hysteresis_tracker = state_dict['hysteresis_tracker']
diff --git a/megatron/core/optimizer/optimizer.py b/megatron/core/optimizer/optimizer.py
old mode 100644
new mode 100755
index 2c33d7e..6358532
--- a/megatron/core/optimizer/optimizer.py
+++ b/megatron/core/optimizer/optimizer.py
@@ -1138,7 +1138,7 @@ class ChainedOptimizer(MegatronOptimizer):
         if self.chained_optimizers:
             return self.chained_optimizers[0].get_loss_scale()
         else:
-            return torch.tensor([1.0], dtype=torch.float32, device=torch.cuda.current_device())
+            return torch.tensor([1.0], dtype=torch.float32, device=torch.npu.current_device())
 
     def _split_state_dict(self, state_dict):
         """Split the state dict into sub-state dicts according to the chunks of each sub-optimizer
diff --git a/megatron/core/parallel_state.py b/megatron/core/parallel_state.py
old mode 100644
new mode 100755
index 0bcea56..999d1ad
--- a/megatron/core/parallel_state.py
+++ b/megatron/core/parallel_state.py
@@ -928,7 +928,7 @@ def initialize_model_parallel(
         # Therefore, we need to perform a nccl call to ensure that the communicator group is created.
         torch.distributed.barrier(
             group=get_data_parallel_group(with_context_parallel=True),
-            device_ids=[torch.cuda.current_device()],
+            device_ids=[torch.npu.current_device()],
         )
         torch.cuda.synchronize()
         # Set `NCCL_COLLNET_ENABLE=0` to restrict SHARP application to the dp group.
@@ -1314,7 +1314,7 @@ def initialize_model_parallel(
                 if _INTER_PARTIAL_EXPERT_DATA_PARALLEL_GROUP is not None:
                     torch.distributed.barrier(
                         group=_INTER_PARTIAL_EXPERT_DATA_PARALLEL_GROUP,
-                        device_ids=[torch.cuda.current_device()],
+                        device_ids=[torch.npu.current_device()],
                     )
                     torch.cuda.synchronize()
                 # Set NCCL_COLLNET_ENABLE to 0 to restrict SHARP application to the dp_replica group.
diff --git a/megatron/core/pipeline_parallel/bridge_communicator.py b/megatron/core/pipeline_parallel/bridge_communicator.py
old mode 100644
new mode 100755
index f1e74a2..305c997
--- a/megatron/core/pipeline_parallel/bridge_communicator.py
+++ b/megatron/core/pipeline_parallel/bridge_communicator.py
@@ -354,7 +354,7 @@ class BridgeCommunicator:
             for src_rank, shape in zip(rank_info.recv_from_ranks, recv_forward_shapes):
                 tensor_to_recv = torch.empty(
                     shape,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     dtype=self.comm_dtype,
                     requires_grad=True,
                 )
@@ -389,7 +389,7 @@ class BridgeCommunicator:
             and self.current_rank in self.dest_grid_broadcast_ranks
         ):
             # Non-leader rank - participate in broadcast
-            shape_tensor = torch.empty((3), device=torch.cuda.current_device(), dtype=torch.int64)
+            shape_tensor = torch.empty((3), device=torch.npu.current_device(), dtype=torch.int64)
             dist.broadcast(
                 shape_tensor, src=self.dest_local_leader_rank, group=self.dest_grid_broadcast_pg
             )
@@ -397,7 +397,7 @@ class BridgeCommunicator:
             received_shape = tuple(shape_tensor.tolist())
             received_tensor = torch.empty(
                 received_shape,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
                 dtype=self.comm_dtype,
                 requires_grad=True,
             )
@@ -483,7 +483,7 @@ class BridgeCommunicator:
             for dest_rank, grad_shape in zip(rank_info.send_to_ranks, recv_grad_shapes):
                 # The destination rank that we sent to will send us gradients back
                 grad_tensor = torch.empty(
-                    grad_shape, device=torch.cuda.current_device(), dtype=self.comm_dtype
+                    grad_shape, device=torch.npu.current_device(), dtype=self.comm_dtype
                 )
                 dist.recv(grad_tensor, src=dest_rank)
                 logging.debug(
@@ -501,7 +501,7 @@ class BridgeCommunicator:
             )
 
             shape_tensor = torch.tensor(
-                aggregated_gradient.shape, device=torch.cuda.current_device(), dtype=torch.int64
+                aggregated_gradient.shape, device=torch.npu.current_device(), dtype=torch.int64
             )
             dist.broadcast(shape_tensor, src=self.current_rank, group=self.src_grid_broadcast_pg)
 
@@ -516,7 +516,7 @@ class BridgeCommunicator:
         ):
             # Non-leader rank - participate in gather for gradients
             # Receive broadcasted tensor shape from leader rank
-            shape_tensor = torch.empty((3), device=torch.cuda.current_device(), dtype=torch.int64)
+            shape_tensor = torch.empty((3), device=torch.npu.current_device(), dtype=torch.int64)
             dist.broadcast(
                 shape_tensor, src=self.src_local_leader_rank, group=self.src_grid_broadcast_pg
             )
@@ -527,7 +527,7 @@ class BridgeCommunicator:
             )
             received_shape = tuple(shape_tensor.tolist())
             received_gradient = torch.empty(
-                received_shape, device=torch.cuda.current_device(), dtype=self.comm_dtype
+                received_shape, device=torch.npu.current_device(), dtype=self.comm_dtype
             )
 
             dist.broadcast(
@@ -586,7 +586,7 @@ class BridgeCommunicator:
                 received_gradients_list = []
                 for i, recv_grad_shape in enumerate(recv_grad_shapes):
                     grad_tensor = torch.empty(
-                        recv_grad_shape, device=torch.cuda.current_device(), dtype=self.comm_dtype
+                        recv_grad_shape, device=torch.npu.current_device(), dtype=self.comm_dtype
                     )
                     received_gradients_list.append(grad_tensor)
 
@@ -623,7 +623,7 @@ class BridgeCommunicator:
                 # Broadcast tensor shape to all ranks in scatter_pg
                 tensor_shape_to_broadcast = aggregated_gradient.shape
                 shape_tensor = torch.tensor(
-                    tensor_shape_to_broadcast, device=torch.cuda.current_device(), dtype=torch.int64
+                    tensor_shape_to_broadcast, device=torch.npu.current_device(), dtype=torch.int64
                 )
                 dist.broadcast(
                     shape_tensor, src=self.current_rank, group=self.src_grid_broadcast_pg
@@ -641,7 +641,7 @@ class BridgeCommunicator:
         ):
             # participate in both gather for gradients
             # Receive gradient from leader using broadcast
-            shape_tensor = torch.empty((3), device=torch.cuda.current_device(), dtype=torch.int64)
+            shape_tensor = torch.empty((3), device=torch.npu.current_device(), dtype=torch.int64)
             dist.broadcast(
                 shape_tensor, src=self.src_local_leader_rank, group=self.src_grid_broadcast_pg
             )
@@ -649,7 +649,7 @@ class BridgeCommunicator:
             # Use the received shape to create tensor for broadcast
             received_shape = tuple(shape_tensor.tolist())
             received_gradient = torch.empty(
-                received_shape, device=torch.cuda.current_device(), dtype=self.comm_dtype
+                received_shape, device=torch.npu.current_device(), dtype=self.comm_dtype
             )
             dist.broadcast(
                 received_gradient, src=self.src_local_leader_rank, group=self.src_grid_broadcast_pg
@@ -704,7 +704,7 @@ class BridgeCommunicator:
                 for i, recv_forward_shape in enumerate(recv_forward_shapes):
                     activation_tensor = torch.empty(
                         recv_forward_shape,
-                        device=torch.cuda.current_device(),
+                        device=torch.npu.current_device(),
                         dtype=self.comm_dtype,
                         requires_grad=True,
                     )
@@ -746,7 +746,7 @@ class BridgeCommunicator:
                 # Broadcast tensor shape to all ranks in scatter_pg
                 tensor_shape_to_scatter = aggregated_activation.shape
                 shape_tensor = torch.tensor(
-                    tensor_shape_to_scatter, device=torch.cuda.current_device(), dtype=torch.int64
+                    tensor_shape_to_scatter, device=torch.npu.current_device(), dtype=torch.int64
                 )
                 dist.broadcast(
                     shape_tensor, src=self.current_rank, group=self.dest_grid_broadcast_pg
@@ -762,7 +762,7 @@ class BridgeCommunicator:
             rank_info.role == CommRole.MEMBER
             and self.current_rank in self.dest_grid_broadcast_ranks
         ):
-            shape_tensor = torch.empty((3), device=torch.cuda.current_device(), dtype=torch.int64)
+            shape_tensor = torch.empty((3), device=torch.npu.current_device(), dtype=torch.int64)
             dist.broadcast(
                 shape_tensor, src=self.dest_local_leader_rank, group=self.dest_grid_broadcast_pg
             )
@@ -771,7 +771,7 @@ class BridgeCommunicator:
             received_shape = tuple(shape_tensor.tolist())
             received_activation = torch.empty(
                 received_shape,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
                 dtype=self.comm_dtype,
                 requires_grad=True,
             )
@@ -829,7 +829,7 @@ class BridgeCommunicator:
             if tensor_to_send_next is not None:
                 send_shape = tensor_to_send_next.shape
                 send_shape_tensor = torch.tensor(
-                    send_shape, device=torch.cuda.current_device(), dtype=torch.int64
+                    send_shape, device=torch.npu.current_device(), dtype=torch.int64
                 )
                 # Add send operations for each destination
                 for dest_rank in rank_info.send_to_ranks:
@@ -843,7 +843,7 @@ class BridgeCommunicator:
             if recv_next:
                 for dest_rank in rank_info.send_to_ranks:
                     grad_shape_tensor = torch.empty(
-                        (3), device=torch.cuda.current_device(), dtype=torch.int64
+                        (3), device=torch.npu.current_device(), dtype=torch.int64
                     )
                     recv_grad_shape_tensors.append(grad_shape_tensor)
                     ops.append(
@@ -857,7 +857,7 @@ class BridgeCommunicator:
             if recv_prev:
                 for src_rank in rank_info.recv_from_ranks:
                     forward_shape_tensor = torch.empty(
-                        (3), device=torch.cuda.current_device(), dtype=torch.int64
+                        (3), device=torch.npu.current_device(), dtype=torch.int64
                     )
                     recv_forward_shape_tensors.append(forward_shape_tensor)
                     ops.append(
@@ -871,7 +871,7 @@ class BridgeCommunicator:
 
                 grad_shape = tensor_to_send_prev.shape
                 grad_shape_tensor = torch.tensor(
-                    grad_shape, device=torch.cuda.current_device(), dtype=torch.int64
+                    grad_shape, device=torch.npu.current_device(), dtype=torch.int64
                 )
 
                 for src_rank in rank_info.recv_from_ranks:
diff --git a/megatron/core/pipeline_parallel/hybrid_cp_schedule.py b/megatron/core/pipeline_parallel/hybrid_cp_schedule.py
old mode 100644
new mode 100755
index 27b5fc8..89079e6
--- a/megatron/core/pipeline_parallel/hybrid_cp_schedule.py
+++ b/megatron/core/pipeline_parallel/hybrid_cp_schedule.py
@@ -518,7 +518,7 @@ def hybrid_context_parallel_forward_backward(
             )
 
     def _broadcast_num_samples_this_group(num_samples_this_group):
-        dev = torch.cuda.current_device()
+        dev = torch.npu.current_device()
         torch.distributed.barrier()
 
         n = 0 if num_samples_this_group is None else int(num_samples_this_group.numel())
diff --git a/megatron/core/pipeline_parallel/p2p_communication.py b/megatron/core/pipeline_parallel/p2p_communication.py
old mode 100644
new mode 100755
index ac839c2..8df7af7
--- a/megatron/core/pipeline_parallel/p2p_communication.py
+++ b/megatron/core/pipeline_parallel/p2p_communication.py
@@ -187,19 +187,19 @@ class P2PCommunicator:
         send_next_shape_tensor = None
         if recv_prev:
             recv_prev_shape_tensor = torch.empty(
-                (3,), device=torch.cuda.current_device(), dtype=torch.int64
+                (3,), device=torch.npu.current_device(), dtype=torch.int64
             )
         if recv_next:
             recv_next_shape_tensor = torch.empty(
-                (3,), device=torch.cuda.current_device(), dtype=torch.int64
+                (3,), device=torch.npu.current_device(), dtype=torch.int64
             )
         if tensor_send_prev is not None:
             send_prev_shape_tensor = torch.tensor(
-                tensor_send_prev.size(), device=torch.cuda.current_device(), dtype=torch.int64
+                tensor_send_prev.size(), device=torch.npu.current_device(), dtype=torch.int64
             )
         if tensor_send_next is not None:
             send_next_shape_tensor = torch.tensor(
-                tensor_send_next.size(), device=torch.cuda.current_device(), dtype=torch.int64
+                tensor_send_next.size(), device=torch.npu.current_device(), dtype=torch.int64
             )
 
         if config.use_ring_exchange_p2p:
@@ -310,7 +310,7 @@ class P2PCommunicator:
             return torch.empty(
                 recv_prev_shape,
                 requires_grad=True,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
                 dtype=config.pipeline_dtype,
             )
 
@@ -318,7 +318,7 @@ class P2PCommunicator:
             return torch.empty(
                 recv_next_shape,
                 requires_grad=True,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
                 dtype=config.pipeline_dtype,
             )
 
diff --git a/megatron/core/rerun_state_machine.py b/megatron/core/rerun_state_machine.py
old mode 100644
new mode 100755
index 8fce2be..487a45c
--- a/megatron/core/rerun_state_machine.py
+++ b/megatron/core/rerun_state_machine.py
@@ -522,7 +522,7 @@ class RerunStateMachine:
                 )
                 rank: int = safe_get_rank()
                 node: str = os.uname()[1]
-                device: int = torch.cuda.current_device()
+                device: int = torch.npu.current_device()
                 full_message: str = (
                     f"Rank {rank}, node {node}, device {device}, "
                     f"iteration {self.current_iteration}: "
@@ -564,7 +564,7 @@ class RerunStateMachine:
         def log_failure(message: str, fatal: bool = True) -> None:
             rank: int = safe_get_rank()
             node: str = os.uname()[1]
-            device: int = torch.cuda.current_device()
+            device: int = torch.npu.current_device()
             if fatal:
                 logger.error(
                     f"Rank {rank}, node {node}, device {device}, "
@@ -641,7 +641,7 @@ class RerunStateMachine:
                     # Remember the node and device we're running on so that we can check we're not
                     # rerunning on the same GPU when we resume from the checkpoint.
                     self.suspicious_node = os.uname()[1]
-                    self.suspicious_device = torch.cuda.current_device()
+                    self.suspicious_device = torch.npu.current_device()
                     self._log_validation_error_to_file(
                         status=RerunValidationStatus.FIRST_RERUN_REPRODUCIBLE,
                         result=result,
@@ -657,7 +657,7 @@ class RerunStateMachine:
             elif self.state == RerunState.RERUNNING_FROM_CHECKPOINT:
                 # Ensure we're not on the same GPU as the first rerun.
                 node = os.uname()[1]
-                device = torch.cuda.current_device()
+                device = torch.npu.current_device()
                 if node == self.suspicious_node and device == self.suspicious_device:
                     logger.error(
                         f"Got rescheduled on the same GPU. Need to resume again from the same "
@@ -954,7 +954,7 @@ class RerunStateMachine:
                 "random_rng_state": random.getstate(),
                 "np_rng_state": np.random.get_state(),
                 "torch_rng_state": torch.get_rng_state(),
-                "cuda_rng_state": torch.cuda.get_rng_state(),
+                "cuda_rng_state": torch.npu.get_rng_state(),
             },
             "other_state": self.state_save_func() if self.state_save_func else None,
             # any other state to save to guarantee deterministic execution?
@@ -967,7 +967,7 @@ class RerunStateMachine:
         random.setstate(rng_state["random_rng_state"])
         np.random.set_state(rng_state["np_rng_state"])
         torch.set_rng_state(rng_state["torch_rng_state"])
-        torch.cuda.set_rng_state(rng_state["cuda_rng_state"])
+        torch.npu.set_rng_state(rng_state["cuda_rng_state"])
         if self.saved_state["other_state"] and self.state_restore_func:
             self.state_restore_func(self.saved_state["other_state"])
 
@@ -1006,7 +1006,7 @@ class RerunStateMachine:
             try:
                 rank: int = safe_get_rank()
                 node: str = os.uname()[1]
-                device: int = torch.cuda.current_device()
+                device: int = torch.npu.current_device()
                 with open(self.result_rejected_tracker_filename, "a") as f:
                     f.write(
                         f"ts={datetime.datetime.now()} node={node} device={device} "
diff --git a/megatron/core/resharding/nvshmem_copy_service/core/gpu_resource_manager.py b/megatron/core/resharding/nvshmem_copy_service/core/gpu_resource_manager.py
old mode 100644
new mode 100755
index 6e03b91..5520f08
--- a/megatron/core/resharding/nvshmem_copy_service/core/gpu_resource_manager.py
+++ b/megatron/core/resharding/nvshmem_copy_service/core/gpu_resource_manager.py
@@ -69,7 +69,7 @@ class GPUResourceManager:
             )
 
         # Get current CUDA device (already set by caller based on LOCAL_RANK)
-        local_rank = torch.cuda.current_device()
+        local_rank = torch.npu.current_device()
 
         # nvshmem4py requires a cuda.core Device at init time
         self.device = Device(local_rank)
diff --git a/megatron/core/ssm/gated_delta_net.py b/megatron/core/ssm/gated_delta_net.py
old mode 100644
new mode 100755
index 70e7497..0cd8e1e
--- a/megatron/core/ssm/gated_delta_net.py
+++ b/megatron/core/ssm/gated_delta_net.py
@@ -170,7 +170,7 @@ class GatedDeltaNet(MegatronModule):
             kernel_size=self.conv_kernel_dim,
             groups=self.conv_dim_local_tp,
             padding=self.conv_kernel_dim - 1,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
             dtype=config.params_dtype,
         )
         setattr(self.conv1d.weight, "tensor_model_parallel", True)
@@ -184,7 +184,7 @@ class GatedDeltaNet(MegatronModule):
             torch.empty(
                 self.num_v_heads_local_tp,
                 dtype=config.params_dtype,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         )
         setattr(self.dt_bias, "tensor_model_parallel", True)
@@ -193,7 +193,7 @@ class GatedDeltaNet(MegatronModule):
             torch.empty(
                 self.num_v_heads_local_tp,
                 dtype=config.params_dtype,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         )
         setattr(self.A_log, "tensor_model_parallel", True)
@@ -236,13 +236,13 @@ class GatedDeltaNet(MegatronModule):
                     self.num_v_heads_local_tp,
                     out=self.dt_bias.data,
                     dtype=self.config.params_dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 )
                 # A_log
                 A = torch.empty(
                     self.num_v_heads_local_tp,
                     dtype=self.config.params_dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 ).uniform_(*self.A_init_range)
                 self.A_log.data.copy_(torch.log(A))
 
@@ -379,7 +379,7 @@ class GatedDeltaNet(MegatronModule):
                 use_qk_l2norm_in_kernel=False,
             )
         else:
-            core_attn_out, last_recurrent_state = chunk_gated_delta_rule(
+            core_attn_out, last_recurrent_state = torch_chunk_gated_delta_rule(
                 query,
                 key,
                 value,
diff --git a/megatron/core/ssm/mamba_mixer.py b/megatron/core/ssm/mamba_mixer.py
old mode 100644
new mode 100755
index cc71cdc..39039c0
--- a/megatron/core/ssm/mamba_mixer.py
+++ b/megatron/core/ssm/mamba_mixer.py
@@ -300,7 +300,7 @@ class MambaMixer(MegatronModule):
                 kernel_size=d_conv,
                 groups=conv_dim,
                 padding=d_conv - 1,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
                 dtype=config.params_dtype,
             )
             setattr(self.conv1d.weight, "tensor_model_parallel", True)
@@ -319,7 +319,7 @@ class MambaMixer(MegatronModule):
             dt = torch.exp(
                 torch.rand(
                     self.nheads_local_tp,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     dtype=config.params_dtype,
                 )
                 * (math.log(dt_max) - math.log(dt_min))
@@ -333,7 +333,7 @@ class MambaMixer(MegatronModule):
             # A parameter
             assert A_init_range[0] > 0 and A_init_range[1] >= A_init_range[0]
             A = torch.empty(
-                self.nheads_local_tp, dtype=torch.float32, device=torch.cuda.current_device()
+                self.nheads_local_tp, dtype=torch.float32, device=torch.npu.current_device()
             )
             if self.config.perform_initialization:
                 A = A.uniform_(*A_init_range)
@@ -345,7 +345,7 @@ class MambaMixer(MegatronModule):
         self.D = nn.Parameter(
             torch.ones(
                 self.d_inner_local_tp if self.D_has_hdim else self.nheads_local_tp,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         )  # Keep in fp32
         setattr(self.D, "tensor_model_parallel", True)
@@ -357,7 +357,7 @@ class MambaMixer(MegatronModule):
                 eps=1e-5,
                 group_size=self.d_inner_local_tp // self.ngroups_local_tp,
                 norm_before_gate=self.norm_before_gate,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
                 dtype=config.params_dtype,
             )
             setattr(self.norm.weight, "tensor_model_parallel", True)
diff --git a/megatron/core/tensor_parallel/data.py b/megatron/core/tensor_parallel/data.py
old mode 100644
new mode 100755
index 6288e6a..e885e1d
--- a/megatron/core/tensor_parallel/data.py
+++ b/megatron/core/tensor_parallel/data.py
@@ -83,7 +83,7 @@ def broadcast_data(keys, data, datatype, tp_group=None):
         # Flatten the data associated with the keys
         flatten_data = torch.cat([data[key].cuda().contiguous().view(-1) for key in keys], dim=0)
     else:
-        flatten_data = torch.empty(total_numel, device=torch.cuda.current_device(), dtype=datatype)
+        flatten_data = torch.empty(total_numel, device=torch.npu.current_device(), dtype=datatype)
 
     # Broadcast
     group_ranks = torch.distributed.get_process_group_ranks(group=tp_group)
diff --git a/megatron/core/tensor_parallel/layers.py b/megatron/core/tensor_parallel/layers.py
old mode 100644
new mode 100755
index 69f442e..30ec7cb
--- a/megatron/core/tensor_parallel/layers.py
+++ b/megatron/core/tensor_parallel/layers.py
@@ -68,11 +68,11 @@ try:
         custom_fwd = partial(torch.amp.custom_fwd, device_type="cuda")
         custom_bwd = partial(torch.amp.custom_bwd, device_type="cuda")
     else:
-        custom_fwd = torch.cuda.amp.custom_fwd
-        custom_bwd = torch.cuda.amp.custom_bwd
+        custom_fwd = torch.amp.custom_fwd(device_type='npu')
+        custom_bwd = torch.amp.custom_bwd(device_type='npu')
 except:
-    custom_fwd = torch.cuda.amp.custom_fwd
-    custom_bwd = torch.cuda.amp.custom_bwd
+    custom_fwd = torch.amp.custom_fwd(device_type='npu')
+    custom_bwd = torch.amp.custom_bwd(device_type='npu')
 
 try:
     if is_torch_min_version("1.13.0"):
@@ -258,7 +258,7 @@ class VocabParallelEmbedding(torch.nn.Module):
                 torch.empty(
                     self.num_embeddings_per_partition,
                     self.embedding_dim,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     dtype=config.params_dtype,
                 )
             )
@@ -549,7 +549,7 @@ class LinearWithGradAccumulationAndAsyncCommunication(torch.autograd.Function):
             assert not ctx.allreduce_dgrad
             dim_size = list(input.size())
             sub_grad_input = torch.empty(
-                dim_size, dtype=input.dtype, device=torch.cuda.current_device(), requires_grad=False
+                dim_size, dtype=input.dtype, device=torch.npu.current_device(), requires_grad=False
             )
             # reduce_scatter
             handle = dist_reduce_scatter_func(
@@ -594,7 +594,7 @@ class LinearWithGradAccumulationAndAsyncCommunication(torch.autograd.Function):
                         grad_weight = torch.zeros(
                             weight.main_grad.shape,
                             dtype=input.dtype,
-                            device=torch.cuda.current_device(),
+                            device=torch.npu.current_device(),
                             requires_grad=False,
                         )
                 else:
@@ -604,7 +604,7 @@ class LinearWithGradAccumulationAndAsyncCommunication(torch.autograd.Function):
                         grad_weight = torch.empty(
                             weight.main_grad.shape,
                             dtype=input.dtype,
-                            device=torch.cuda.current_device(),
+                            device=torch.npu.current_device(),
                             requires_grad=False,
                         )
                 weight.grad_added_to_main_grad = True
@@ -870,7 +870,7 @@ class ColumnParallelLinear(torch.nn.Module):
                     torch.empty(
                         self.output_size_per_partition,
                         self.input_size,
-                        device=torch.cuda.current_device(),
+                        device=torch.npu.current_device(),
                         dtype=config.params_dtype,
                     )
                 )
@@ -896,7 +896,7 @@ class ColumnParallelLinear(torch.nn.Module):
                 self.bias = Parameter(
                     torch.empty(
                         self.output_size_per_partition,
-                        device=torch.cuda.current_device(),
+                        device=torch.npu.current_device(),
                         dtype=config.params_dtype,
                     )
                 )
@@ -1194,7 +1194,7 @@ class RowParallelLinear(torch.nn.Module):
                 torch.empty(
                     self.output_size,
                     self.input_size_per_partition,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     dtype=config.params_dtype,
                 )
             )
@@ -1215,7 +1215,7 @@ class RowParallelLinear(torch.nn.Module):
                 self.bias = Parameter(
                     torch.empty(
                         self.output_size,
-                        device=torch.cuda.current_device(),
+                        device=torch.npu.current_device(),
                         dtype=config.params_dtype,
                     )
                 )
diff --git a/megatron/core/tensor_parallel/mappings.py b/megatron/core/tensor_parallel/mappings.py
old mode 100644
new mode 100755
index 9ff69c9..98fe014
--- a/megatron/core/tensor_parallel/mappings.py
+++ b/megatron/core/tensor_parallel/mappings.py
@@ -88,7 +88,7 @@ def _gather_along_last_dim(input_, group):
     dim_size = list(input_.size())
     dim_size[0] = dim_size[0] * world_size
 
-    output = torch.empty(dim_size, dtype=input_.dtype, device=torch.cuda.current_device())
+    output = torch.empty(dim_size, dtype=input_.dtype, device=torch.npu.current_device())
     dist_all_gather_func(output, input_.contiguous(), group=group)
     tensor_list = output.chunk(world_size, dim=0)
     output = torch.cat(tensor_list, dim=-1).contiguous()
@@ -138,14 +138,14 @@ def _gather_along_first_dim(input_, group, output_split_sizes=None, use_global_b
         if use_global_buffer:
             output = get_global_memory_buffer().get_tensor(dim_size, input_.dtype, "mpu")
         else:
-            output = torch.empty(dim_size, dtype=input_.dtype, device=torch.cuda.current_device())
+            output = torch.empty(dim_size, dtype=input_.dtype, device=torch.npu.current_device())
         dist_all_gather_func(output, input_.contiguous(), group=group)
     else:
         dim_size[0] = sum(output_split_sizes)
         if use_global_buffer:
             output = get_global_memory_buffer().get_tensor(dim_size, input_.dtype, "mpu")
         else:
-            output = torch.empty(dim_size, dtype=input_.dtype, device=torch.cuda.current_device())
+            output = torch.empty(dim_size, dtype=input_.dtype, device=torch.npu.current_device())
         output_tensor_list = list(torch.split(output, output_split_sizes, dim=0))
         torch.distributed.all_gather(output_tensor_list, input_, group=group)
 
@@ -178,7 +178,7 @@ def _reduce_scatter_along_first_dim(input_, group, input_split_sizes=None, use_g
         if use_global_buffer:
             output = get_global_memory_buffer().get_tensor(dim_size, input_.dtype, "mpu")
         else:
-            output = torch.empty(dim_size, dtype=input_.dtype, device=torch.cuda.current_device())
+            output = torch.empty(dim_size, dtype=input_.dtype, device=torch.npu.current_device())
         dist_reduce_scatter_func(output, input_.contiguous(), group=group)
     else:
         rank = group.rank()
@@ -439,7 +439,7 @@ class _AllToAll(torch.autograd.Function):
             output = input.new_empty(
                 size=[sum(output_split_sizes)] + list(input.size()[1:]),
                 dtype=input.dtype,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         torch.distributed.all_to_all_single(
             output,
diff --git a/megatron/core/tensor_parallel/random.py b/megatron/core/tensor_parallel/random.py
old mode 100644
new mode 100755
index bf00717..c6634d7
--- a/megatron/core/tensor_parallel/random.py
+++ b/megatron/core/tensor_parallel/random.py
@@ -61,7 +61,7 @@ def _get_cuda_rng_state(
         device = torch.device("cuda", device)
     idx = device.index
     if idx is None:
-        idx = torch.cuda.current_device()
+        idx = torch.npu.current_device()
 
     default_generator = torch.cuda.default_generators[idx]
     if clone:
@@ -99,7 +99,7 @@ def _set_cuda_rng_state(new_state: torch.Tensor, device: int = -1, graph_safe: b
         def cb():
             idx = device.index
             if idx is None:
-                idx = torch.cuda.current_device()
+                idx = torch.npu.current_device()
             default_generator = torch.cuda.default_generators[idx]
 
             # if graph capturing, set the rng state in a cudagraphable way
@@ -232,10 +232,10 @@ class CudaRNGStatesTracker:
             self.states_[name] = new_state
         else:
             # Get the current rng state.
-            orig_rng_state = torch.cuda.get_rng_state()
+            orig_rng_state = torch.npu.get_rng_state()
             # Set the new state and store it.
-            torch.cuda.manual_seed(seed)
-            self.states_[name] = torch.cuda.get_rng_state()
+            torch.npu.manual_seed(seed)
+            self.states_[name] = torch.npu.get_rng_state()
             # Reset rng state to what it was.
             _set_cuda_rng_state(orig_rng_state)
 
@@ -419,7 +419,7 @@ def model_parallel_cuda_manual_seed(
     )
     _CUDA_RNG_STATE_TRACKER.reset()
     # Set the default state.
-    torch.cuda.manual_seed(data_parallel_seed)
+    torch.npu.manual_seed(data_parallel_seed)
     _CUDA_RNG_STATE_TRACKER.add(_DATA_PARALLEL_RNG_TRACKER_NAME, data_parallel_seed)
 
     # and model parallel state.
diff --git a/megatron/core/tensor_parallel/utils.py b/megatron/core/tensor_parallel/utils.py
old mode 100644
new mode 100755
index ad467a9..2637386
--- a/megatron/core/tensor_parallel/utils.py
+++ b/megatron/core/tensor_parallel/utils.py
@@ -67,7 +67,7 @@ def split_tensor_into_1d_equal_chunks(tensor, new_buffer=False, tp_group=None):
         data = torch.empty(
             partition_size,
             dtype=tensor.dtype,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
             requires_grad=False,
         )
         data.copy_(tensor.view(-1)[start_index:end_index])
@@ -88,7 +88,7 @@ def gather_split_1d_tensor(tensor, tp_group=None):
     tp_group = get_tensor_model_parallel_group_if_none(tp_group)
     numel_gathered = torch.numel(tensor) * tp_group.size()
     gathered = torch.empty(
-        numel_gathered, dtype=tensor.dtype, device=torch.cuda.current_device(), requires_grad=False
+        numel_gathered, dtype=tensor.dtype, device=torch.npu.current_device(), requires_grad=False
     )
     dist_all_gather_func(gathered, tensor, group=tp_group)
     return gathered
diff --git a/megatron/core/timers.py b/megatron/core/timers.py
old mode 100644
new mode 100755
index 78204ad..c57bfd6
--- a/megatron/core/timers.py
+++ b/megatron/core/timers.py
@@ -300,7 +300,7 @@ class Timers:
         # and since we are only gathering a small amount of data,
         # it should be ok to use all-gather instead of gather.
         rank_name_to_time = torch.zeros(
-            (world_size, len(names)), dtype=torch.float, device=torch.cuda.current_device()
+            (world_size, len(names)), dtype=torch.float, device=torch.npu.current_device()
         )
         for i, name in enumerate(names):
             if name in self._timers:
diff --git a/megatron/core/transformer/attention.py b/megatron/core/transformer/attention.py
old mode 100644
new mode 100755
index 2200b55..f0a98a4
--- a/megatron/core/transformer/attention.py
+++ b/megatron/core/transformer/attention.py
@@ -427,7 +427,7 @@ class Attention(MegatronModule, ABC):
             self.num_query_groups_per_partition,
             dim,
             dtype=dtype,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
 
     def _get_pp_layer_offset_for_inference(self):
diff --git a/megatron/core/transformer/dot_product_attention.py b/megatron/core/transformer/dot_product_attention.py
old mode 100644
new mode 100755
index 69039e0..6b0da2f
--- a/megatron/core/transformer/dot_product_attention.py
+++ b/megatron/core/transformer/dot_product_attention.py
@@ -120,7 +120,7 @@ class DotProductAttention(MegatronModule):
         elif self.config.softmax_type == "off-by-one":
             self.softmax_offset = torch.zeros(
                 self.num_attention_heads_per_partition,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
                 dtype=self.config.params_dtype,
             )
         elif self.config.softmax_type == "learnable":
@@ -129,7 +129,7 @@ class DotProductAttention(MegatronModule):
                 torch.nn.Parameter(
                     torch.empty(
                         self.num_attention_heads_per_partition,
-                        device=torch.cuda.current_device(),
+                        device=torch.npu.current_device(),
                         dtype=self.config.params_dtype,
                     )
                 ),
diff --git a/megatron/core/transformer/experimental_attention_variant/dsa.py b/megatron/core/transformer/experimental_attention_variant/dsa.py
old mode 100644
new mode 100755
index 88b4713..71c3d22
--- a/megatron/core/transformer/experimental_attention_variant/dsa.py
+++ b/megatron/core/transformer/experimental_attention_variant/dsa.py
@@ -74,7 +74,7 @@ class DSAIndexerLossLoggingHelper:
 
         tracker = DSAIndexerLossLoggingHelper.tracker
         if "values" not in tracker:
-            tracker["values"] = torch.zeros(num_layers, device=torch.cuda.current_device())
+            tracker["values"] = torch.zeros(num_layers, device=torch.npu.current_device())
         tracker["values"][layer_number - 1] += loss.detach()
         tracker["reduce_group"] = reduce_group
         tracker["avg_group"] = avg_group
diff --git a/megatron/core/transformer/module.py b/megatron/core/transformer/module.py
old mode 100644
new mode 100755
index c30c107..f29d3d7
--- a/megatron/core/transformer/module.py
+++ b/megatron/core/transformer/module.py
@@ -246,7 +246,7 @@ class GraphableMegatronModule(MegatronModule):
             (slen_per_cptp, micro_batch_size, self.config.hidden_size),
             dtype=torch.bfloat16,
             requires_grad=True,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         return static_inputs
 
diff --git a/megatron/core/transformer/moe/experts.py b/megatron/core/transformer/moe/experts.py
old mode 100644
new mode 100755
index 62fb7a1..6ae05c2
--- a/megatron/core/transformer/moe/experts.py
+++ b/megatron/core/transformer/moe/experts.py
@@ -181,7 +181,7 @@ class GroupedMLP(MegatronModule):
                 torch.empty(
                     self.config.hidden_size,
                     fc1_output_size_per_partition,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     dtype=config.params_dtype,
                 )
             )
@@ -189,7 +189,7 @@ class GroupedMLP(MegatronModule):
                 torch.empty(
                     fc2_input_size_per_partition,
                     self.config.hidden_size,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     dtype=config.params_dtype,
                 )
             )
diff --git a/megatron/core/transformer/moe/router.py b/megatron/core/transformer/moe/router.py
old mode 100644
new mode 100755
index 4be9740..724aef7
--- a/megatron/core/transformer/moe/router.py
+++ b/megatron/core/transformer/moe/router.py
@@ -87,9 +87,9 @@ class Router(ABC, MegatronModule):
         """
         if self.weight.device.type == 'cpu':
             # move weights to GPU
-            self.weight.data = self.weight.data.to(device=torch.cuda.current_device())
+            self.weight.data = self.weight.data.to(device=torch.npu.current_device())
         if self.bias is not None and self.bias.device.type == 'cpu':
-            self.bias.data = self.bias.data.to(device=torch.cuda.current_device())
+            self.bias.data = self.bias.data.to(device=torch.npu.current_device())
 
         # Convert to specified datatype for routing computation if enabled
         router_dtype = input.dtype
@@ -166,7 +166,7 @@ class TopKRouter(Router):
                 torch.zeros(
                     self.config.num_moe_experts,
                     dtype=torch.float32,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 ),
                 persistent=False,
             )
@@ -175,7 +175,7 @@ class TopKRouter(Router):
                 torch.zeros(
                     self.config.num_moe_experts,
                     dtype=torch.float32,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 ),
             )
         else:
@@ -189,13 +189,13 @@ class TopKRouter(Router):
                 torch.zeros(
                     self.config.num_moe_experts,
                     dtype=torch.float32,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 ),
                 persistent=False,
             )
             self.register_buffer(
                 'ga_steps',
-                torch.tensor(0, dtype=torch.float32, device=torch.cuda.current_device()),
+                torch.tensor(0, dtype=torch.float32, device=torch.npu.current_device()),
                 persistent=False,
             )
         else:
diff --git a/megatron/core/transformer/multi_token_prediction.py b/megatron/core/transformer/multi_token_prediction.py
index 2edb652..b0be369 100755
--- a/megatron/core/transformer/multi_token_prediction.py
+++ b/megatron/core/transformer/multi_token_prediction.py
@@ -176,7 +176,7 @@ def roll_tensor(tensor, shifts=-1, dims=-1, cp_group=None, packed_seq_params=Non
         empty_tensor = torch.empty(
             tensor_send_list[i].shape,
             dtype=tensor_send_list[i].dtype,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         tensor_recv_list.append(empty_tensor)
 
@@ -334,7 +334,7 @@ class MTPLossLoggingHelper:
 
         tracker = MTPLossLoggingHelper.tracker
         if "values" not in tracker:
-            tracker["values"] = torch.zeros(num_layers, device=torch.cuda.current_device())
+            tracker["values"] = torch.zeros(num_layers, device=torch.npu.current_device())
         tracker["values"][layer_number] += loss.detach()
         tracker["reduce_group"] = reduce_group
         tracker["avg_group"] = avg_group
diff --git a/megatron/core/transformer/transformer_layer.py b/megatron/core/transformer/transformer_layer.py
old mode 100644
new mode 100755
index a5eaec9..abd8db2
--- a/megatron/core/transformer/transformer_layer.py
+++ b/megatron/core/transformer/transformer_layer.py
@@ -933,7 +933,7 @@ class TransformerLayer(GraphableMegatronModule, BaseTransformerLayer):
             slen_per_cp = seq_length // self.config.context_parallel_size
             static_inputs["attention_mask"] = (
                 ~(torch.tril(torch.ones((slen_per_cp, seq_length))).bool())
-                .to(torch.cuda.current_device())
+                .to(torch.npu.current_device())
                 .reshape(1, 1, slen_per_cp, seq_length)
                 .tile(micro_batch_size, 1, 1, 1)
             )
@@ -1144,7 +1144,7 @@ class TransformerLayer(GraphableMegatronModule, BaseTransformerLayer):
                 return torch.zeros(
                     (micro_batch_size, 1, slen_per_cp, slen),
                     dtype=torch.bool,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                 )
 
             if not is_te_min_version("1.10.0"):
diff --git a/megatron/core/utils.py b/megatron/core/utils.py
old mode 100644
new mode 100755
index f8d068c..3d4099b
--- a/megatron/core/utils.py
+++ b/megatron/core/utils.py
@@ -628,7 +628,7 @@ class GlobalMemoryBuffer:
                 self.buffer[(name, dtype)] = torch.empty(
                     required_len,
                     dtype=dtype,
-                    device=torch.cuda.current_device(),
+                    device=torch.npu.current_device(),
                     requires_grad=False,
                 )
 
@@ -1467,7 +1467,7 @@ class StragglerDetector:
             self.stop_data_tm = []
             backend = torch.distributed.get_backend()
             if backend == "nccl":
-                self.dev = torch.cuda.current_device()
+                self.dev = torch.npu.current_device()
             else:
                 self.dev = torch.device("cpu")
             # cache some events
diff --git a/megatron/legacy/model/bert_model.py b/megatron/legacy/model/bert_model.py
old mode 100644
new mode 100755
index 451667d..1fc98a8
--- a/megatron/legacy/model/bert_model.py
+++ b/megatron/legacy/model/bert_model.py
@@ -200,7 +200,7 @@ class BertModel(MegatronModule):
                 output = torch.zeros(
                     size=(embeddings.shape[0], embeddings.shape[2]),
                     dtype=torch.float32,
-                    device=torch.cuda.current_device())
+                    device=torch.npu.current_device())
                 for i, (embedding, mask) in enumerate(zip(embeddings, masks)):
                     output[i, :] = torch.mean(embedding[1: mask - 1], dim=0)
 
diff --git a/megatron/legacy/model/transformer.py b/megatron/legacy/model/transformer.py
old mode 100644
new mode 100755
index ca3414e..286b503
--- a/megatron/legacy/model/transformer.py
+++ b/megatron/legacy/model/transformer.py
@@ -236,7 +236,7 @@ class SwitchMLP(MegatronModule):
 
         # TODO pre allocate memory
         output = torch.empty(dim_size, dtype=local_indices.dtype,
-                             device=torch.cuda.current_device())
+                             device=torch.npu.current_device())
         torch.distributed._all_gather_base(
             output, local_indices.contiguous(), group=self.tp_ep_group
         )
@@ -652,7 +652,7 @@ class ParallelAttention(MegatronModule):
             num_attention_heads,
             self.hidden_size_per_attention_head,
             dtype=self.params_dtype,
-            device=torch.cuda.current_device())
+            device=torch.npu.current_device())
 
     def forward(self, hidden_states, attention_mask,
                 encoder_output=None, inference_context=None,
diff --git a/megatron/legacy/mpu/tests/test_random.py b/megatron/legacy/mpu/tests/test_random.py
old mode 100644
new mode 100755
index 2609277..6656415
--- a/megatron/legacy/mpu/tests/test_random.py
+++ b/megatron/legacy/mpu/tests/test_random.py
@@ -19,11 +19,11 @@ def test_set_cuda_rng_state(tensor_model_parallel_size):
 
     size = 123
     seed = 1234
-    torch.cuda.manual_seed(1234)
+    torch.npu.manual_seed(1234)
     tensor = torch.tensor(size, dtype=torch.float, device='cuda')
 
     # Get the state
-    rng_state = torch.cuda.get_rng_state()
+    rng_state = torch.npu.get_rng_state()
     rng_state_copy = rng_state.clone()
 
     # Do some stuff.
@@ -32,10 +32,10 @@ def test_set_cuda_rng_state(tensor_model_parallel_size):
     result_1 = tensor.clone()
 
     assert rng_state.sub(rng_state_copy).max() == 0
-    assert torch.cuda.get_rng_state().sub(rng_state_copy).max() > 0
+    assert torch.npu.get_rng_state().sub(rng_state_copy).max() > 0
 
     # State should be different.
-    new_rng_state = torch.cuda.get_rng_state()
+    new_rng_state = torch.npu.get_rng_state()
     max_diff = new_rng_state.sub(rng_state).max()
     print('   max diff in rng state (should be non-zero) on global rank {}: {}'.
           format(torch.distributed.get_rank(), max_diff))
@@ -85,14 +85,14 @@ def test_cuda_rng_tracker(tensor_model_parallel_size):
     tensor = torch.tensor(size, dtype=torch.float, device='cuda')
 
     # Set to seed_1 and generate two tensors.
-    torch.cuda.manual_seed(seed_1)
+    torch.npu.manual_seed(seed_1)
     torch.randn(size, out=tensor)
     target_11 = tensor.clone()
     torch.randn(size, out=tensor)
     target_12 = tensor.clone()
 
     # Set to seed_2 and generate two tensors.
-    torch.cuda.manual_seed(seed_2)
+    torch.npu.manual_seed(seed_2)
     torch.randn(size, out=tensor)
     target_21 = tensor.clone()
     torch.randn(size, out=tensor)
@@ -100,7 +100,7 @@ def test_cuda_rng_tracker(tensor_model_parallel_size):
 
     # Now if we interleave seed_1 and seed_2,
     # we should still get the same tensors
-    torch.cuda.manual_seed(seed_1)
+    torch.npu.manual_seed(seed_1)
     mpu.get_cuda_rng_tracker().add('test', seed_2)
 
     torch.randn(size, out=tensor)
diff --git a/megatron/post_training/non_loss_data_func.py b/megatron/post_training/non_loss_data_func.py
old mode 100644
new mode 100755
index d5daf62..947ed14
--- a/megatron/post_training/non_loss_data_func.py
+++ b/megatron/post_training/non_loss_data_func.py
@@ -32,7 +32,7 @@ def report_draft_acceptance_length(model, osl: int = 64, draft_steps: int = 7):
     for category, conversations in category_and_prompt.items():
         input_ids = tokenizer.apply_chat_template(
             conversations, return_tensors="pt", add_generation_prompt=True
-        ).to(torch.cuda.current_device())
+        ).to(torch.npu.current_device())
         output_ids, actual_osl, steps = simple_speculative_generate(
             unwrapped_model, input_ids, osl=osl, steps=draft_steps, disable_tqdm=True
         )
diff --git a/megatron/rl/rl_utils.py b/megatron/rl/rl_utils.py
old mode 100644
new mode 100755
index 364a80d..e3a6994
--- a/megatron/rl/rl_utils.py
+++ b/megatron/rl/rl_utils.py
@@ -104,7 +104,7 @@ def _maybe_prefetch_separate_inference_model_weights(model_core, *, to_cpu: bool
         return
     if args.rl_inference_model_unified_memory_level != 1:
         return
-    device = -1 if to_cpu else int(torch.cuda.current_device())
+    device = -1 if to_cpu else int(torch.npu.current_device())
     # Note: include_buffers=False because buffers created with explicit device= in register_buffer()
     # are not allocated via the UVM mempool and will fail UVM operations. Only parameters are UVM-allocated.
     advise_managed_module_parameters_preferred_location(model_core, device=device, include_buffers=False)
@@ -155,7 +155,7 @@ def verify_model_weights_swap(
 
     actual_vocab_size = getattr(args, 'padded_vocab_size', 128256)
     actual_seq_len = min(seq_len, getattr(args, 'seq_length', seq_len))
-    device = torch.device(f"cuda:{torch.cuda.current_device()}")
+    device = torch.device(f"cuda:{torch.npu.current_device()}")
 
     # Generate deterministic test input - same across ALL ranks
     torch.manual_seed(1234)
@@ -1047,7 +1047,7 @@ def _compute_logprobs_batch(
             trajs_batch_size,
             seq_length-1,
             dtype=dtype,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
 
     # Only PP>1 needs a broadcast from the last stage; for PP=1 the output is already local.
diff --git a/megatron/training/checkpointing.py b/megatron/training/checkpointing.py
old mode 100644
new mode 100755
index a3d307f..b145672
--- a/megatron/training/checkpointing.py
+++ b/megatron/training/checkpointing.py
@@ -340,7 +340,7 @@ def get_rng_state(ckpt_format: str, tp_group: torch.distributed.ProcessGroup, pp
         'random_rng_state': random.getstate(),
         'np_rng_state': np.random.get_state(),
         'torch_rng_state': torch.get_rng_state(),
-        'cuda_rng_state': torch.cuda.get_rng_state(),
+        'cuda_rng_state': torch.npu.get_rng_state(),
         'rng_tracker_states': tensor_parallel.get_cuda_rng_tracker().get_states()}
 
     rng_state_list = None
@@ -1863,7 +1863,7 @@ def load_checkpoint(ddp_model, optimizer, opt_param_scheduler, load_arg='load',
                 random.setstate(rng_state['random_rng_state'])
                 np.random.set_state(rng_state['np_rng_state'])
                 torch.set_rng_state(rng_state['torch_rng_state'])
-                torch.cuda.set_rng_state(rng_state['cuda_rng_state'])
+                torch.npu.set_rng_state(rng_state['cuda_rng_state'])
                 # Check for empty states array
                 if not rng_state['rng_tracker_states']:
                     raise KeyError
@@ -1875,7 +1875,7 @@ def load_checkpoint(ddp_model, optimizer, opt_param_scheduler, load_arg='load',
                 random.setstate(state_dict['random_rng_state'])
                 np.random.set_state(state_dict['np_rng_state'])
                 torch.set_rng_state(state_dict['torch_rng_state'])
-                torch.cuda.set_rng_state(state_dict['cuda_rng_state'])
+                torch.npu.set_rng_state(state_dict['cuda_rng_state'])
                 # Check for empty states array
                 if not state_dict['rng_tracker_states']:
                     raise KeyError
diff --git a/megatron/training/ft_integration.py b/megatron/training/ft_integration.py
old mode 100644
new mode 100755
index f3532e7..11192e2
--- a/megatron/training/ft_integration.py
+++ b/megatron/training/ft_integration.py
@@ -328,7 +328,7 @@ def maybe_setup_simulated_fault() -> None:
     rank = torch.distributed.get_rank()
     rand_rank = rng.randint(0, torch.distributed.get_world_size() - 1)
     rank_to_fail = rank_to_fail if rank_to_fail is not None else rand_rank
-    rank_to_fail = torch.tensor([rank_to_fail], device=torch.cuda.current_device())
+    rank_to_fail = torch.tensor([rank_to_fail], device=torch.npu.current_device())
     torch.distributed.broadcast(rank_to_fail, 0)
     rank_to_fail = int(rank_to_fail.item())
 
diff --git a/megatron/training/initialize.py b/megatron/training/initialize.py
old mode 100644
new mode 100755
index c150ac3..efe4a9a
--- a/megatron/training/initialize.py
+++ b/megatron/training/initialize.py
@@ -142,7 +142,7 @@ def initialize_megatron(
         if args.num_experts is not None:
             from megatron.core.transformer.moe.router import MoEAuxLossAutoScaler
 
-            MoEAuxLossAutoScaler.set_loss_scale(torch.ones(1, device=torch.cuda.current_device()))
+            MoEAuxLossAutoScaler.set_loss_scale(torch.ones(1, device=torch.npu.current_device()))
 
     if skip_mpu_initialization:
         return None
diff --git a/megatron/training/training.py b/megatron/training/training.py
old mode 100644
new mode 100755
index d61b935..db743e1
--- a/megatron/training/training.py
+++ b/megatron/training/training.py
@@ -1255,7 +1255,7 @@ def get_model(model_provider_func, model_type=ModelType.encoder_or_decoder, wrap
         and not args.init_model_with_meta_device
     ):
         for model_module in model:
-            model_module.cuda(torch.cuda.current_device())
+            model_module.cuda(torch.npu.current_device())
 
     # Fp16 conversion.
     if args.fp16 or args.bf16:
diff --git a/megatron/training/utils.py b/megatron/training/utils.py
old mode 100644
new mode 100755
index 01897c1..6cf20ce
--- a/megatron/training/utils.py
+++ b/megatron/training/utils.py
@@ -250,7 +250,7 @@ def reduce_max_stat_across_model_parallel_group(stat: float) -> float | None:
     """
     if stat is None:
         stat = -1.0
-    stat = torch.tensor([stat], dtype=torch.float32, device=torch.cuda.current_device())
+    stat = torch.tensor([stat], dtype=torch.float32, device=torch.npu.current_device())
     torch.distributed.all_reduce(
         stat, op=torch.distributed.ReduceOp.MAX, group=mpu.get_model_parallel_group()
     )
@@ -269,7 +269,7 @@ def logical_and_across_model_parallel_group(input: bool) -> bool:
         input = 1
     else:
         input = 0
-    input = torch.tensor([input], dtype=torch.int, device=torch.cuda.current_device())
+    input = torch.tensor([input], dtype=torch.int, device=torch.npu.current_device())
     torch.distributed.all_reduce(
         input, op=torch.distributed.ReduceOp.MIN, group=mpu.get_model_parallel_group()
     )
@@ -445,6 +445,8 @@ def is_first_or_last_pipeline_stage(vp_stage):
 
 def get_device_arch_version():
     """Returns GPU arch version (8: Ampere, 9: Hopper, 10: Blackwell, ...)"""
+    if hasattr(torch, 'npu') and torch.npu.is_available():
+        return 10  # NPU: treat as Blackwell to avoid CUDA restrictions
     return torch.cuda.get_device_properties(torch.device("cuda:0")).major
 
 
@@ -558,7 +560,7 @@ def get_batch_on_this_tp_rank(data_iterator, mtp_on_this_rank: bool = False):
         }
 
         def _broadcast_cu_seqlens(cu_seqlens):
-            dev = torch.cuda.current_device()
+            dev = torch.npu.current_device()
             n = 0 if cu_seqlens is None else int(cu_seqlens.numel())
             n_tensor = torch.tensor(n, dtype=torch.int64, device=dev)
             _broadcast(n_tensor)
@@ -573,7 +575,7 @@ def get_batch_on_this_tp_rank(data_iterator, mtp_on_this_rank: bool = False):
             _broadcast(buf)
 
         if args.hybrid_context_parallel:
-            seq_len = torch.tensor(batch['tokens'].shape[0], dtype=torch.int32, device=torch.cuda.current_device())
+            seq_len = torch.tensor(batch['tokens'].shape[0], dtype=torch.int32, device=torch.npu.current_device())
             _broadcast(seq_len)
             
         if args.pipeline_model_parallel_size == 1 or mtp_on_this_rank:
@@ -602,7 +604,7 @@ def get_batch_on_this_tp_rank(data_iterator, mtp_on_this_rank: bool = False):
             _broadcast(batch['attention_mask'])
 
         def _broadcast_cu_seqlens(cu_seqlens):
-            dev = torch.cuda.current_device()
+            dev = torch.npu.current_device()
 
             n = 0 if cu_seqlens is None else int(cu_seqlens.numel())
             n_tensor = torch.tensor(n, dtype=torch.int64, device=dev)
@@ -622,7 +624,7 @@ def get_batch_on_this_tp_rank(data_iterator, mtp_on_this_rank: bool = False):
 
     else:
         if args.hybrid_context_parallel:
-            seq_len = torch.tensor(0, dtype=torch.int32, device=torch.cuda.current_device())
+            seq_len = torch.tensor(0, dtype=torch.int32, device=torch.npu.current_device())
             _broadcast(seq_len)
             shape = (seq_len.item())
         else:
@@ -631,38 +633,38 @@ def get_batch_on_this_tp_rank(data_iterator, mtp_on_this_rank: bool = False):
         tokens = torch.empty(
             shape,
             dtype=torch.int64,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         labels = torch.empty(
             shape,
             dtype=torch.int64,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         loss_mask = torch.empty(
             shape,
             dtype=torch.float32,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         if args.create_attention_mask_in_dataloader:
             shape_attention_mask = (args.micro_batch_size, 1, args.seq_length, args.seq_length) if not args.hybrid_context_parallel else (1, 1, shape[0], shape[0])
             attention_mask = torch.empty(
                 shape_attention_mask,
                 dtype=torch.bool,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         else:
             attention_mask = None
         position_ids = torch.empty(
             shape,
             dtype=torch.int64,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         )
         cu_seqlens = None
         if args.sft:
             max_seqlen = torch.empty(
                 1,
                 dtype=torch.int32,
-                device=torch.cuda.current_device(),
+                device=torch.npu.current_device(),
             )
         else:
             max_seqlen = None
@@ -671,16 +673,16 @@ def get_batch_on_this_tp_rank(data_iterator, mtp_on_this_rank: bool = False):
         max_seqlen = torch.empty(
             1,
             dtype=torch.int32,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         ) if args.hybrid_context_parallel else None
         local_cp_size = torch.empty(
             1,
             dtype=torch.int32,
-            device=torch.cuda.current_device(),
+            device=torch.npu.current_device(),
         ) if args.hybrid_context_parallel else None
 
         def _broadcast_cu_seqlens():
-            dev = torch.cuda.current_device()
+            dev = torch.npu.current_device()
 
             n = torch.empty((), dtype=torch.int64, device=dev)
             _broadcast(n)
@@ -728,7 +730,7 @@ def get_batch_on_this_tp_rank(data_iterator, mtp_on_this_rank: bool = False):
             _broadcast(attention_mask)
 
         def _broadcast_cu_seqlens():
-            dev = torch.cuda.current_device()
+            dev = torch.npu.current_device()
 
             n = torch.empty((), dtype=torch.int64, device=dev)
             _broadcast(n)

diff --git a/megatron/core/transformer/transformer_config.py b/megatron/core/transformer/transformer_config.py
index eaae585..52a49ba 100644
--- a/megatron/core/transformer/transformer_config.py
+++ b/megatron/core/transformer/transformer_config.py
@@ -906,6 +906,129 @@ class TransformerConfig(ModelParallelConfig):
     min_offloaded_tensor_size: int = 1024 * 1024
     """The minimum size of the tensor to be offloaded."""
 
+    MINDSPEED_DEFAULTS = {
+        "multi_head_latent_attention": False,
+        "multi_latent_attention": False,
+        "use_fused_rotary_pos_emb": False,
+        "pre_tockens": 65536,
+        "post_tockens": 65536,
+        "next_tockens": 65536,
+        "context_parallel_algo": "ulysses_cp_algo",
+        "tp_2d": False,
+        "tp_x": 1,
+        "tp_y": 1,
+        "is_llava": False,
+        "reset_attention_mask": False,
+        "attention_mask_type": "causal",
+        "moe_zero_memory_num_layers": None,
+        "share_embeddings_and_output_weights": False,
+        "moe_zero_memory": "disable",
+        "moe_allgather_overlap_comm": False,
+        "moe_alltoall_overlap_comm": False,
+        "moe_apply_probs_on_input": False,
+        "moe_expert_capacity_factor": None,
+        "moe_grouped_gemm": False,
+        "moe_pad_expert_input_to_capacity": False,
+        "moe_permute_fusion": False,
+        "moe_router_pre_softmax": False,
+        "moe_router_topk": 2,
+        "moe_router_topk_scaling_factor": 1.0,
+        "moe_shared_expert_intermediate_size": None,
+        "moe_shared_expert_overlap": False,
+        "moe_token_dispatcher_type": None,
+        "moe_token_drop_policy": None,
+        "moe_tp_extend_ep": False,
+        "n_shared_experts": None,
+        "num_experts": None,
+        "num_moe_experts": None,
+        "balanced_moe_hot_expert_num": None,
+        "sparse_mode": 0,
+        "window_size": None,
+        "with_shared_expert": False,
+        "shared_expert_gate": False,
+        "use_cp_send_recv_overlap": False,
+        "use_ulysses_allgather_kv": False,
+        "context_parallel_cache_interval": 0,
+        "context_parallel_kv_cache_policy": None,
+        "megatron_cp_in_bnsd": False,
+        "add_extra_token_to_sequence": False,
+        "create_attention_mask": True,
+        "alibi_diagonal_opposite": False,
+        "alibi_fusion_attn_type": None,
+        "coc_fused_kernel": False,
+        "coc_mode": None,
+        "coc_parallel_num": 1,
+        "coc_row_nocomm": False,
+        "enable_cuda_graph": False,
+        "external_cuda_graph": False,
+        "enable_overlap_ag_with_matmul": False,
+        "enable_overlap_matmul_with_rs": False,
+        "enable_backward_overlap_ag_with_matmul": False,
+        "enable_executor": False,
+        "use_fused_rmsnorm": False,
+        "use_fusion_attn_v2": False,
+        "use_precision_aware_optimizer": False,
+        "use_quant_optimizer": False,
+        "add_qkv_bias": False,
+        "q_lora_rank": None,
+        "kv_lora_rank": None,
+        "qk_head_dim": None,
+        "v_head_dim": None,
+        "qk_pos_emb_head_dim": None,
+        "mscale": None,
+        "mscale_all_dim": None,
+        "rope_type": None,
+        "rotary_scaling_factor": None,
+        "barrier_with_L1_time": False,
+        "bwd_op_layer_info": None,
+        "fwd_op_layer_info": None,
+        "current_stage_op_id": None,
+        "one_step_duration": None,
+        "micro_batch_num": None,
+        "step": None,
+        "stage": None,
+        "timers": None,
+        "tokenizer": None,
+        "is_oom": False,
+        "activation_func_fp8_input_store": False,
+        "gemm_gradient_accumulation_fusion": False,
+        "bias_activation_fusion": False,
+        "bias_dropout_fusion": False,
+        "defer_embedding_wgrad_compute": False,
+        "distribute_saved_activations": False,
+        "enable_profiler": False,
+        "enable_custom_record_stream": False,
+        "clip_grad": None,
+        "reuse_fp32_param": False,
+        "schedules_method": None,
+        "transformer_impl": "local",
+        "cpu_offloading": False,
+        "_cpu_offloading_context": None,
+        "eod_mask_loss": False,
+        "log_num_zeros_in_grad": False,
+        "deterministic_mode": False,
+        "apply_query_key_layer_scaling": False,
+        "layernorm_zero_centered_gamma": False,
+        "perform_initialization": True,
+        "use_cpu_initialization": False,
+        "output_layer_init_method": None,
+        "init_method": None,
+        "beta_fast": None,
+        "beta_slow": None,
+        "cache_mla_latents": False,
+        "seq_length": None,
+        "micro_batch_size": None,
+        "max_position_embeddings": None,
+        "rotary_percent": 1.0,
+        "recompute_modules": None,
+        "reset_position_ids": False,
+    }
+
+    def __getattr__(self, name):
+        if name in TransformerConfig.MINDSPEED_DEFAULTS:
+            return TransformerConfig.MINDSPEED_DEFAULTS[name]
+        raise AttributeError(f"'{type(self).__name__}' object has no attribute '{name}'")
+
     def __post_init__(self):
         """Python dataclass method that is used to modify attributes after initialization.
         See https://docs.python.org/3/library/dataclasses.html#post-init-processing for more
